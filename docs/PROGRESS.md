# Progresso

> Atualizado ao final de cada etapa de trabalho. Escrito para ser lido por qualquer pessoa da equipe.
> **Última atualização:** 2026-08-28 — **a barra lateral administrativa voltou para a coluna da esquerda** (**D-86**): dez classes escritas na sintaxe do Tailwind 3 geravam CSS que o navegador descartava, a barra ficava flutuando por cima do conteúdo e cobria o próprio botão de recolher. Corrigido, com **duas provas novas** — um cenário de navegador em tela grande e uma varredura do CSS construído. ⚠️ **De quebra, descobriu-se que o passo de testes do CI nunca rodou um teste sequer** desde o commit de bootstrap: ele chamava `./vendor/bin/phpunit`, que morre antes de carregar a suíte. Consertado no mesmo commit. Antes dela, Etapa 22 (**a troca da biblioteca de primitivas**: `radix-vue` saiu do projeto e entrou **`reka-ui` 2** nos 23 componentes de interface; **nenhuma tela mudou de aparência** — o que muda é comportamento; **DA-44** e **DA-45**). ✅ **O plano B foi fechado**, e com ele as duas armadilhas do `components.json`: o gerador oficial do shadcn voltou a servir (§14 do `docs/ARCHITECTURE.md`, reescrita). ✅ **O defeito herdado da Etapa 21 que ficou registrado aqui foi corrigido depois** (**D-86**): as classes `w-[--reka-...]` de `NavUser.vue` e `NavigationMenuViewport.vue` passaram para a forma `w-(--reka-...)` do Tailwind 4, junto com outras oito ocorrências que ninguém tinha visto — inclusive a que quebrava a barra lateral do painel. Antes dela, Etapa 21 (**Tailwind 4 e o tema do shadcn studio**: o projeto subiu de Tailwind 3.4 para 4, o `tailwind.config.js` deixou de existir e a paleta passou a ser a do studio — o botão "Fazer inscrição" **deixou de ser vermelho**; **DA-39 a DA-43**). Antes dela, Etapa 20 (a **porta da rua**: a raiz do domínio deixou de ser a tela de exemplo do Laravel e passou a apresentar o evento, com os três estados — um aberto, vários, nenhum — cada um com teste; **DA-34 a DA-38**). Antes dela, Etapa 19 (implantação: o sistema virou **uma imagem Docker** publicada no GHCR e uma **stack de cinco containers** para o Portainer, com `app`, `worker` e `scheduler` separados, atrás do Traefik). **Fases 0 a 9 concluídas, mais a etapa de empacotamento.** ✅ **A pendência mais antiga do projeto foi fechada:** o trabalhador da fila deixou de depender de alguém lembrar de subi-lo — agora é o container `worker`, com reinício automático. ⚠️ **O sistema ainda não está no ar, e o que falta não é código**: mesclar em `main` para o workflow publicar a imagem, criar a stack no Portainer, criar o primeiro administrador por comando, cadastrar a credencial da Efí pela tela e registrar o endereço do aviso na Efí — a lista completa, na ordem, está em `docs/DEPLOY.md`. ⚠️ **Nada foi ligado contra dinheiro de verdade.** ⚠️ **A revisão de LGPD NÃO foi feita** — não há retenção, prazo de descarte nem anonimização, e isso depende das pendências **P-04** e **P-03**. É o maior buraco conhecido do projeto. ⚠️ **Estorno não existe** — depende da política de reembolso (**P-02**). ⚠️ **A taxa efetiva da Efí não foi confirmada com o comercial** (**P-06**). ⚠️ **O mTLS de verdade no aviso da Efí ficou fora** (**DA-28**): a defesa é HMAC mais lista de IP, e é desvio consciente do que a Efí recomenda.

---

## Concluído

- [x] Etapa 1 — `docs/PRD.md` com as 24 seções exigidas + Glossário; `docs/PROGRESS.md`
- [x] Etapa 2 — `docs/ARCHITECTURE.md`, `docs/DATABASE.md` (ERD + dicionário de dados), `docs/BUSINESS_RULES.md` (RN-01 a RN-13 + regras de pagamento)
- [x] Etapa 3 — `docs/PAYMENTS.md` (matriz de provedores com data de consulta) e `docs/IMPLEMENTATION_PLAN.md`; revisão cruzada dos 7 documentos
- [x] Etapa 4 — Base do projeto: Laravel 12 com pacote inicial Vue (Inertia + TypeScript + Tailwind), Laravel Sail com PostgreSQL 18, Redis e Mailpit, `config/payments.php`, fuso `America/Sao_Paulo`, Pest e Pint funcionando. Migrações do framework aplicadas no PostgreSQL e 27 testes do pacote inicial passando

- [x] Etapa 5 — Domínio do evento: migrações de `cidades`, `grupos_participantes`, `eventos`, `dias_evento`, `grupos_atividades`, `atividades` e `conflitos_atividades` com todas as restrições `CHECK` e índices; Enum `SituacaoEvento`; sete modelos em português com relações, conversões e filtros

- [x] Etapa 6 — Fábricas de dados para os sete modelos, `CidadeSeeder`, `EventoDemoSeeder` (Copa CCC 2026, dois dias) e `tests/Feature/Dominio/EventoTest.php` com 29 testes provando relações, filtros e as restrições do banco

- [x] Etapa 7 — Inscrição: migrações de `inscricoes` (com as unicidades parciais de e-mail e CPF) e `inscricoes_atividades`; Enum `SituacaoInscricao`; modelos `Inscricao` e `InscricaoAtividade`; `DadosNovaInscricao`; `ValidadorSelecaoAtividades` (RN-03 a RN-08); `ReservarVagas` e `LiberarVagas` (contador atômico em ordem fixa); `CriarInscricao` (transação única, varredura sob demanda com uma retentativa, tradução das violações de unicidade em mensagem amigável); `ExpirarInscricoesVencidas` na versão mínima; `StoreInscricaoRequest`, `InscricaoController@store` e a rota `POST /inscricoes`; anúncio interno `InscricaoCriada`

- [x] Etapa 8 — Testes das regras de inscrição: `tests/Feature/Inscricoes/` com `InscricaoTest`, `SelecaoAtividadesTest`, `ConflitoAtividadeTest`, `CapacidadeAtividadeTest`, `InscricaoDuplicadaTest` e `ConcorrenciaTest`, apoiados no cenário compartilhado `Cenario.php`. A concorrência é provada de duas formas: pela gravação condicional (o `UPDATE` não altera nenhuma linha quando o contador alcançou a capacidade) e por seis processos de sistema operacional de verdade disputando a última vaga ao mesmo tempo, cada um com a sua própria conexão (`scripts/disputar-vaga.php`). **Suíte completa: 140 testes, 333 asserções, tudo passando**

**Com isso, a Fase 3 — Inscrição está concluída:** regras RN-01 a RN-13, reserva de vaga por contador atômico, varredura sob demanda das reservas vencidas e cobertura de teste dos seis cenários exigidos pelo briefing que dependem apenas do domínio de inscrição.


- [x] Etapa 9 — Pagamento e webhook: migrações `pagamentos` e `webhooks_pagamento` (dinheiro em centavos, unicidade parcial do identificador externo); Enums `SituacaoPagamento`, `MetodoPagamento` e `SituacaoWebhook`; modelos `Pagamento` e `WebhookPagamento`; contrato `PaymentGateway` e DTOs somente leitura (`app/Contracts/Payments`, `app/DTOs/Payments` — em inglês, por espelharem a fronteira externa); `FakePaymentGateway` completo (emite cobrança Pix fictícia no formato EMV, paga, vence, falha, cancela, estorna e emite aviso assinado); `PaymentServiceProvider` escolhendo o provedor por `config/payments.php`; Actions `CriarPagamentoDaInscricao`, `ConfirmarPagamento` e `CancelarPagamento`; `PaymentWebhookController` (confere assinatura, grava o aviso cru sem dado sensível, responde imediatamente) e o job idempotente `ProcessarWebhookPagamento`; `routes/dev.php` com as rotas de simulação, com dupla trava de ambiente e configuração. A criação de inscrição já emite a cobrança, com `expira_em` igual ao `prazo_pagamento`. **Suíte completa: 160 testes, 424 asserções, tudo passando**

- [x] Etapa 10 — Prazo, expiração e reconciliação: `ExpirarInscricoesVencidas` completa (marca a inscrição como expirada, devolve a vaga do evento e a de cada atividade, encerra a cobrança como "prazo vencido" e anuncia `InscricaoExpirada`, tudo em lotes de 100 com `chunkById`); anúncios internos `InscricaoConfirmada` e `InscricaoExpirada`; comandos `inscricoes:expirar-vencidas` (com a opção `--evento`) e `pagamentos:reconciliar` (com as opções `--margem` e `--lote`), agendados em `routes/console.php` a cada minuto e a cada cinco minutos; testes `PrazoPagamentoTest`, `ExpiracaoInscricaoTest` e `ReconciliacaoTest`. **Suíte completa: 177 testes, 521 asserções, tudo passando**

**Com isso, a Fase 4 — Pagamento está concluída**, e o fluxo do briefing foi verificado de ponta a ponta em banco recém-semeado, por comandos artisan: inscrição válida reserva a vaga do evento e a de cada atividade → cobrança Pix fictícia emitida com o mesmo prazo da inscrição → aviso assinado entregue na rota pública (`POST /webhooks/pagamentos` → HTTP 200) → inscrição confirmada com os contadores passando de reservada para confirmada; e, em seguida, uma segunda inscrição com o prazo vencido → `php artisan inscricoes:expirar-vencidas` → vaga do evento e de cada atividade de volta a zero reservadas, cobrança em "prazo vencido", **nenhuma linha apagada** (2 inscrições, 2 pagamentos e 4 vínculos inscrição-atividade continuam no banco) e a segunda execução do comando não muda mais nada.

- [x] Etapa 11 — **Fase 5a, site público**: `EventoPublicoController` (`/eventos/{slug}`, com rascunho e cancelado respondendo 404), `InscricaoPublicaController` (`/eventos/{slug}/inscricao`), sete Resources que montam os props sem vazar documento, hash nem contador cru, `InscricaoController@store` adaptado para redirecionar o Inertia à URL assinada mantendo o JSON de sempre para quem chama por outro caminho, `GeradorQrCodePix` (SVG embutido, sem imagick) e `PagamentoController@show`/`@situacao` atrás do middleware `signed`. No navegador: `PublicoLayout`, a vitrine `Eventos/Show.vue`, o formulário `Inscricoes/Criar.vue` em quatro etapas (dados, participação, revisão, pagamento) com as regras RN-03 a RN-09 espelhadas em `useSelecaoAtividades` — mínimo e máximo, bloco obrigatório, choque de horário, conflito declarado, faixa etária e vaga esgotada — e a tela `Inscricoes/Pagamento.vue` com QR Code, copia e cola, contador regressivo e consulta leve que troca para "inscrição confirmada" sem recarregar. Identidade do CCC em tokens semânticos (vermelho de ação, verde de disponibilidade, azul de informação, amarelo de atenção sempre com texto preto), com contraste AA medido nos modos claro e escuro. **Suíte Pest: 205 testes, 795 asserções. Suíte Playwright: 12 cenários** — caminho feliz, confirmação do pagamento, erro de validação (na tela e vindo do servidor), esgotado, conflito de horário, máximo de seleções, acesso sem assinatura e a varredura de acessibilidade e responsividade (teclado, foco visível, anúncio de troca de etapa, alvos de 44 px e ausência de rolagem horizontal a 320 px)

**Com isso, a Fase 5a está concluída.**

- [x] Etapa 12 — **Fase 5b, área do participante**: quem se inscreve passou a ter um lugar só seu. O link assinado abre `/inscricoes/{código}/acompanhar`, uma página que conta em português simples o que já aconteceu com a inscrição. No alto, o resumo: evento, situação, valor e as atividades escolhidas. Abaixo, a **linha do tempo**, com os oito marcos possíveis — inscrição feita, cobrança Pix emitida, prazo para pagar, pagamento recebido, inscrição confirmada, prazo vencido, inscrição cancelada e valor estornado —, cada um com data e hora e com o estado escrito por extenso ("Concluído", "Agora", "A seguir", "Encerrado") ao lado de um desenho, nunca só por cor. Essa linha do tempo é **derivada, não gravada**: nenhuma tabela nova foi criada, e o serviço `LinhaDoTempoDaInscricao` monta os marcos a partir dos carimbos de tempo que o domínio já mantinha, ordenados por momento e com um único marco marcado como "agora". Em seguida vem o **histórico da cobrança**: todos os pagamentos da inscrição, do mais recente ao mais antigo, com situação, valor, forma de pagamento e datas — e sem nunca mostrar identificador do provedor, dados internos do aviso ou o código Pix fora da tela que ainda aceita pagamento. Enquanto o prazo não venceu, um botão traz a **segunda via do Pix**: ele chama a mesma Action de sempre (`CriarPagamentoDaInscricao`), que devolve a cobrança pendente quando já existe uma e emite outra quando não existe nenhuma — o que também conserta o caso raro em que a emissão original falhou e deixou a inscrição sem cobrança. Vencido o prazo, no lugar do botão aparece a explicação do motivo. E, para quem fechou o navegador e perdeu o link, `/acesso` pede só o e-mail e envia por mensagem a lista das inscrições daquele endereço — nome do evento, situação e o link de cada uma, válido por sete dias, sem CPF, telefone ou valor em aberto. A tela responde sempre a mesma frase, no mesmo tempo, exista ou não inscrição para aquele e-mail. A tela de pagamento e a página do evento ganharam os links que levam até a área do participante. **Suíte Pest: 241 testes, 1048 asserções** — 36 testes e 253 asserções a mais que ao final da Fase 5a. **Suíte Playwright: 21 cenários** — os 12 da 5a, intactos e verdes, mais 9 novos: linha do tempo com o prazo como passo de agora, confirmação aparecendo na linha do tempo, acesso sem assinatura recusado, mesma mensagem com e sem inscrição, e-mail malformado sem revelar existência, segunda via devolvendo a tela de pagamento com QR Code sem criar outra cobrança, e a varredura de acessibilidade das telas novas (320 px, alvos de toque de 44 px, teclado com foco sempre visível e linha do tempo legível sem depender de cor)

**Com isso, a Fase 5b está concluída**, e com ela todo o caminho do participante: descobrir o evento, se inscrever, pagar, acompanhar e voltar. Nenhuma regra de negócio nova foi criada nesta fase — nenhuma Action, nenhum Enum, nenhuma migração —, e o único e-mail que sai do sistema continua sendo o do link de acesso. O **cancelamento da inscrição pela própria participante continua fora do escopo**: é decisão que ainda cabe ao dono do produto. A próxima entrega é a **Fase 6 — Administração**. A pendência **P-09** (os 20 erros de verificação de tipos nas telas do pacote inicial) segue aberta de propósito, para ser resolvida justamente na Fase 6, que reescreve essas telas.

- [x] Etapa 13 — **Fase 6a, acesso administrativo e painel de números**: até aqui o sistema tinha um problema silencioso — qualquer visitante podia criar uma conta na tela de cadastro que veio junto com o pacote inicial do Laravel, e essa conta entrava no lado de dentro sem que ninguém tivesse convidado. Esta etapa fechou essa porta e acendeu a luz.

    **Papéis e permissões.** Entrou o pacote `spatie/laravel-permission`, com **dois papéis**: `administrador`, que responde pelo sistema inteiro, e `organizador`, que toca o evento no dia a dia. As **nove permissões** são nomeadas em português e por assunto (`painel.ver`, `catalogo.gerenciar`, `eventos.gerenciar`, `inscricoes.ver`, `inscricoes.exportar`, `inscricoes.cancelar`, `pagamentos.confirmar-manual`, `usuarios.gerenciar` e `auditoria.ver`). O organizador alcança tudo, **menos** confirmar pagamento na mão, mexer em contas e ler o histórico de auditoria: confirmar pagamento manualmente é a única ação do sistema que declara "entrou dinheiro" sem que fonte externa nenhuma tenha reconhecido nada, e quanto menos gente puder fazer isso, melhor. O `PapeisSeeder` é idempotente — rodar duas vezes não duplica nem tira permissão de quem já tem.

    **Cadastro público fechado.** As rotas `GET /register` e `POST /register` saíram do ar, junto com a tela `Register.vue`, o controller que a atendia e o link "cadastre-se" da tela de login. O teste que existia para provar que o cadastro funcionava foi **reescrito para provar o contrário**: hoje ele exige o 404 nas duas rotas. Um teste que prova que a porta está trancada vale mais do que a ausência de teste. No lugar do cadastro, a conta administrativa nasce por comando: `php artisan usuario:criar-administrador {e-mail} --nome= --papel=`, que pede a senha de forma escondida (ela nunca aparece na tela nem no histórico do terminal), recusa e-mail repetido, confere se o papel existe de verdade e diz em português o que fez. Há também um `AdminDemoSeeder` com uma conta de demonstração que **só nasce em ambiente `local`**, com a mesma dupla trava das rotas de simulação de pagamento (D-29): conta previsível em servidor de verdade é porta aberta com o endereço escrito na placa.

    **Toda rota administrativa exige permissão.** O grupo `/admin` passa por três travas, nesta ordem: estar logado, ter o e-mail confirmado e ter a permissão daquela tela. Estar logado não diz o que a pessoa pode fazer — por isso nenhuma rota do lado de dentro fica só com autenticação.

    **O painel.** Uma tela, um evento por vez, com seletor (o padrão é o evento mais recente que não seja rascunho). Três blocos: **inscrições por situação** (total, confirmadas, aguardando pagamento, expiradas, canceladas e lista de espera, cada uma com a explicação do que aquele número significa); **vagas por atividade** (capacidade, reservadas, confirmadas e restantes, em tabela com cabeçalho de linha e de coluna e legenda); e **dinheiro** (recebido, a receber e estornado, sempre em centavos no servidor e formatado só na tela). Todos os números saem de **consultas agregadas** — o banco conta e soma —, nunca de carregar milhares de inscrições na memória para mostrar quatro números. E a vaga restante é lida do **contador gravado na própria atividade**, que é a fonte da verdade do domínio: recontar as escolhas criaria uma segunda versão do mesmo número, e as duas divergiriam no primeiro caso de borda. Evento publicado que ainda não recebeu ninguém mostra **zeros com explicação**, não tela vazia nem erro.

    **Verificação de tipos limpa pela primeira vez.** A pendência **P-09** — os 20 erros de tipo herdados do pacote inicial — foi fechada aqui, e não adiada de novo, porque é justamente esta fase que passa a usar de verdade aquelas telas. Corrigir foi tipar direito, sem apagar erro com atalho.

    **O painel só lê.** Nenhuma Action, Enum, Model, migração ou evento de domínio foi alterado. **Suíte Pest: 268 testes, 1207 asserções** — 27 testes e 159 asserções a mais que ao final da Fase 5b. **Suíte Playwright: 25 cenários** — os 21 anteriores intactos e verdes, mais 4 novos: visitante mandado para o login, pessoa logada sem papel recebendo a recusa, administrador vendo os três blocos de números e o cadastro público confirmadamente fora do ar.

**Com isso, a Fase 6a está concluída.** A próxima entrega é a **Fase 6b — cadastros e gestão de inscrições**.

- [x] Etapa 14 — **Fase 6b, cadastros do evento e gestão das inscrições**: até aqui quem organiza enxergava os números do evento, mas não podia mexer em nada. Cadastrar uma atividade, achar uma pessoa no meio de todas ou desfazer a inscrição de quem desistiu só acontecia com alguém abrindo o banco de dados por fora. Esta etapa acabou com isso — e, pela primeira vez desde a Fase 4, criou **regra de negócio nova**.

    **Os cadastros pela tela.** O catálogo global (cidades e grupos de participantes) e a estrutura do evento (o evento em si, seus dias, os grupos de atividades, as atividades e os conflitos de horário entre elas) agora se cadastram, editam e excluem pela tela. A regra que mais aparece nessas telas é a de **não apagar o que já está em uso**: o banco recusa isso por conta própria, mas recusar com erro de banco é devolver a quem usa uma frase que ninguém entende. Por isso cada restrição do banco foi **espelhada em português antes**, no formulário: "Esta cidade não pode ser excluída porque tem 3 grupo(s) de participantes ligado(s) a ela", data final que não pode vir antes da inicial, capacidade que não pode ficar abaixo do que já está reservado mais confirmado, par de conflito que é normalizado e não se repete, atividade que não conflita consigo mesma, mínimo de participantes que não pode passar do máximo. E mexer na estrutura de um evento **que já tem gente inscrita** é recusado com explicação e com o caminho certo em seguida (encerrar as inscrições, cancelar as inscrições, criar outro evento), em vez de passar em silêncio e estragar a conta de vagas de quem já se inscreveu.

    **A lista de inscrições.** Filtros que se combinam entre si — evento, situação da inscrição, cidade, grupo de participantes, atividade escolhida, situação do pagamento e período de criação — mais busca por nome, e-mail e código público. A paginação (25 por página, da mais recente para a mais antiga) leva os filtros junto, para que a página 2 continue mostrando a mesma pergunta da página 1. **CPF não filtra e não busca**: o documento é guardado cifrado e a impressão digital dele só serve para comparar o número inteiro, nunca um pedaço. Quem digitar "123" não vai achar ninguém, e isso é de propósito — uma busca por pedaço de CPF seria uma máquina de descobrir quem está inscrito. O CPF também não aparece em coluna nenhuma da lista.

    **A exportação para planilha.** Um botão que gera o CSV **respeitando exatamente os filtros que estão na tela**: o que a pessoa vê é o que ela leva. O arquivo sai **em fluxo, linha por linha**, com um cursor aberto no banco — um evento com dez mil inscritos não vira dez mil registros empilhados na memória do servidor antes de o download começar. O separador é ponto e vírgula e o arquivo leva o BOM UTF-8, que é o que faz o Excel em português abrir a planilha com as colunas separadas e os acentos no lugar. E, de novo, **sem CPF**: a planilha nasce para circular por e-mail e por pasta compartilhada, e o documento de ninguém viaja junto.

    **A ficha de uma inscrição.** Abre com o resumo, as atividades escolhidas e o **histórico completo da cobrança** — cada pagamento, com situação, valor, forma e datas. É dali que saem as duas ações administrativas.

    **As duas ações novas — o coração desta fase.** Cancelar uma inscrição e reconhecer um pagamento na mão são as duas únicas coisas que o lado de dentro faz que mexem em **vaga** e em **dinheiro**. Por isso receberam o mesmo rigor que a criação da inscrição recebeu lá na Fase 3.

    O **cancelamento administrativo** (`CancelarInscricaoAdministrativa`) segue linha a linha o molde da rotina de expiração, que já era o pedaço de código mais testado do projeto. A mudança de situação é uma **gravação condicional**: o comando só altera a linha se a situação ainda for a esperada. Se ninguém mudou nada no meio do caminho, a vaga volta; se outra aba, outra pessoa ou a própria rotina de expiração chegou primeiro, nenhuma linha é alterada e a ação termina sem efeito, sem erro na tela e — o que importa — **sem devolver a vaga duas vezes**. As vagas voltam na **ordem canônica** de sempre, evento primeiro e depois cada atividade por ordem crescente de identificador, que é o que impede duas transações de ficarem esperando uma pela outra. O anúncio só sai **depois** que a transação fecha, e só na chamada que de fato cancelou. Existe um teste de **concorrência de verdade** — dois processos disputando a mesma inscrição, um cancelando e outro expirando — provando que os contadores de vaga terminam exatamente onde deviam. Nada é apagado: a inscrição continua no banco, agora com a data e o motivo escritos.

    A **confirmação manual de pagamento** (`ConfirmarPagamentoManual`) existe porque isso acontece de verdade: a pessoa paga em dinheiro na secretaria, ou faz uma transferência direta, e nenhum provedor tem como saber disso. É a **única ação do sistema inteiro que afirma "entrou dinheiro" sem que nenhuma fonte externa tenha reconhecido nada** — e por isso é a mais trancada de todas: só o `administrador` alcança, a observação é obrigatória e fica gravado no pagamento que a origem foi manual, com o método declarado e o nome de quem declarou. **Nenhum identificador de provedor é inventado**: se o provedor não emitiu nada, o campo fica vazio, porque preencher seria falsificar histórico de dinheiro. O destino é o mesmo de um pagamento reconhecido pelo provedor, então o caminho também é o mesmo — a ação delega para a `ConfirmarPagamento` que já existia, em vez de repetir a regra por fora.

    **Nenhuma tabela nova, nenhuma migração.** As duas ações escrevem em colunas que já existiam desde a Fase 3. O registro de auditoria continua na Fase 9; os textos de motivo e de observação escritos agora são justamente o material que vai alimentá-lo.

    **As contas.** **Suíte Pest: 370 testes, 1805 asserções** — 102 testes e 598 asserções a mais que ao final da Fase 6a. **Suíte Playwright: 28 cenários** — os 25 anteriores intactos e verdes, mais 3 novos: achar a pessoa pelo filtro, cancelar escrevendo o motivo e ver a vaga voltar no painel; tentar cancelar sem motivo e ser barrado na tela, com o foco voltando para o botão certo depois do `Esc`; e o organizador não enxergando o botão de confirmar pagamento na mão — nem pela tela, nem por pedido feito por fora dela, que responde 403.

**Com isso, a Fase 6 está concluída** (6a e 6b), e o sistema passa a ser operável de ponta a ponta sem ninguém abrir o banco: o participante se inscreve, paga e acompanha; a organização cadastra o evento inteiro, encontra qualquer inscrição, exporta a lista e age sobre um caso concreto. A próxima entrega é a **Fase 7 — Comunicação**, que finalmente vai escutar os anúncios que o domínio já vem disparando há três fases. Duas pendências continuam **abertas de propósito e precisam de decisão do dono do produto**: a **P-02** (política de reembolso) e a **P-03** (o que fazer com pagamento reconhecido depois do prazo). Enquanto elas não forem decididas, o sistema toma em cada caso a leitura mais segura — não devolve dinheiro sozinho e não ressuscita inscrição expirada — e diz isso na tela, em português, para quem estiver operando.

- [x] Etapa 15 — **Fase 7, comunicação por e-mail**: desde a Fase 3 o sistema anunciava para si mesmo tudo o que acontecia com uma inscrição — "foi criada", "foi confirmada", "venceu o prazo", "foi cancelada" — e **ninguém escutava**. Quem se inscrevia só ficava sabendo do que aconteceu se tivesse guardado a aba aberta. Esta etapa ligou os avisos à caixa de entrada da pessoa, e não mudou **uma linha sequer** de regra de inscrição ou de pagamento para isso: foi só plugar quem escuta.

    **Os cinco e-mails.** *Inscrição recebida* (o que foi escolhido, quanto custa, até quando pagar e o link para pagar), *o prazo está acabando*, *pagamento confirmado* (o comprovante, com data, valor, atividades e código da inscrição), *prazo vencido* (o que aconteceu, que a vaga voltou para a fila e o convite a tentar de novo se ainda houver lugar) e *inscrição cancelada*. Todos escritos em português simples, para serem lidos por quem nunca usou o sistema, todos com **versão em texto puro além do HTML** — quem lê e-mail no celular com imagens desligadas, ou num programa antigo, recebe a mesma mensagem inteira. O remetente vem do `.env` e não aparece em lugar nenhum do código.

    **O e-mail leva link, não ficha cadastral.** Nenhum dos cinco corpos contém CPF, a impressão digital do CPF, telefone ou o código Pix inteiro. Mensagem de caixa de entrada é encaminhada, impressa e guardada por anos; o que ela carrega é um **link assinado**, que vence sozinho e pode ser trocado — a mesma regra do e-mail de acesso (D-44). Há um teste que renderiza os cinco corpos, em HTML e em texto puro, e procura por cada um desses dados. E o e-mail de cancelamento **não repassa o motivo interno**: a anotação que o organizador escreve é para a organização, não para a pessoa. A mensagem diz que a inscrição foi cancelada pela organização e mostra o contato do evento — o Mailable do cancelamento nem sequer **recebe** o texto do motivo, então não existe caminho por onde ele possa vazar por descuido.

    **Como a duplicação é impedida — e por que isso é diferente do óbvio.** Antes de mandar qualquer mensagem, o sistema grava uma linha em `comunicacoes_enviadas` dizendo "esta inscrição, esta mensagem, este canal". Existe uma **regra de unicidade no banco de dados** sobre essas três colunas, e é **ela** que impede a segunda cópia — não uma verificação escrita em PHP. A diferença não é preciosismo: o jeito óbvio seria perguntar "já mandei?" e enviar se a resposta fosse não, mas dois trabalhadores rodando ao mesmo tempo perguntam juntos, ouvem "ainda não" os dois, e a pessoa recebe duas cópias. Por isso a ordem aqui é ao contrário do intuitivo: **grava primeiro, envia depois**, tudo na mesma transação. Se o banco recusar a gravação, alguém já mandou e o trabalho encerra em silêncio. Se o envio falhar, a transação volta atrás e o registro some junto, deixando o caminho livre para a próxima tentativa. A prova disso está nos testes: um deles verifica que o índice único existe mesmo no banco; outro mostra que, quando já existe registro, o sistema **ainda assim tenta gravar** e apanha a recusa do banco (o PostgreSQL deixa o rastro: o identificador reservado para a linha recusada não volta); e outro coloca dois trabalhadores com o mesmo trabalho na mão, os dois vendo o caminho livre no mesmo instante, e cobra que saia **uma** mensagem só.

    **O lembrete de prazo.** O comando `inscricoes:lembrar-prazo` roda a cada quinze minutos e avisa quem está aguardando pagamento com prazo vencendo dentro da janela configurada (padrão: as próximas 24 horas). Ele percorre em lotes com `chunkById`, no mesmo molde da rotina de expiração, e a consulta já exclui quem recebeu o aviso. Quem confirmou, quem expirou, quem foi cancelado e quem já perdeu o prazo **não** recebem — para esse último, o aviso é outro. Rodar o comando duas vezes seguidas não manda nada na segunda vez, e isso não depende da sorte do agendador: depende da mesma unicidade no banco.

    **Tudo sai pela fila, e é aqui que mora o aviso importante.** Nenhum e-mail é enviado durante o pedido da pessoa: o sistema deixa o trabalho na fila `emails` e responde na hora. Um servidor de e-mail lento leva segundos para responder, e **nenhuma inscrição pode esperar por isso**. A consequência precisa ser dita sem rodeio: **enquanto ninguém subir o trabalhador da fila, nenhum e-mail sai** — a fila só cresce, sem erro e sem reclamação. Hoje **nenhum trabalhador roda neste projeto**, porque até esta fase nada usava fila. O comando está pronto para copiar em `docs/ARCHITECTURE.md` (seção 9.1), no `.env.example` e aqui: `php artisan queue:work redis --queue=emails` (com Sail, `./vendor/bin/sail artisan queue:work redis --queue=emails`). Em produção ele precisa ser serviço supervisionado, e o agendador continua sendo outro processo. *(Atualização da Etapa 19: isso deixou de ser pendência — em produção o trabalhador é o container `worker` da stack, e o agendador é o container `scheduler`. Na máquina de quem desenvolve o comando acima continua sendo o caminho.)*

    **Quando o e-mail não chega.** São 3 tentativas, com 1, 5 e 15 minutos de espera entre elas — servidor de e-mail fora do ar costuma ser problema de minutos. Esgotadas, o trabalho vai para a tabela `failed_jobs` com o erro completo, e **nada acontece com a inscrição, a vaga ou o pagamento**. Existe teste para isso: com um servidor de e-mail que não existe, o trabalho falha, aparece em `failed_jobs`, e a situação da inscrição, a contagem de vagas e os pagamentos ficam exatamente como estavam. O prejuízo máximo de uma falha de e-mail é um e-mail que não chegou.

    **Um defeito encontrado no caminho.** Ao testar a fila de verdade, apareceu que os e-mails estavam sendo enfileirados na fila `default`, e não na `emails` — ou seja, um trabalhador dedicado a `emails`, exatamente como documentamos, **nunca os veria**. O motivo: para descobrir em qual fila colocar um ouvinte, o Laravel cria uma cópia dele **sem chamar o construtor**, e era o construtor que definia a fila. A correção foi transformar fila, conexão, tentativas e espera em **métodos** (`viaQueue()`, `viaConnection()`, `tries()`, `backoff()`), que funcionam na cópia sem construtor. O teste que trancou isso não pergunta ao objeto bem-comportado: ele monta a mesma cópia sem construtor que o Laravel monta, e confere o que ela responde. Sem fila de verdade no teste, o defeito teria estreado em produção como "os e-mails não chegam e ninguém sabe por quê".

    **Nenhuma tela, nenhuma dependência, uma tabela nova.** A fase é inteira de backend. A única tabela criada é `comunicacoes_enviadas` (`failed_jobs` já existia desde o pacote inicial). Nenhuma Action, Enum de domínio, Model de domínio ou migração existente foi alterada, e nenhum anúncio mudou de lugar ou de momento. WhatsApp **não** foi implementado: o que existe é a coluna `canal` e um vocabulário de tipos de mensagem — o desenho que não precisará ser desfeito quando (e se) um segundo meio de aviso entrar.

    **As contas.** **Suíte Pest: 407 testes, 2036 asserções** — 37 testes e 231 asserções a mais que ao final da Fase 6b. **Suíte Playwright: 28 cenários**, os mesmos de antes, intactos e verdes: esta fase não criou tela nenhuma e não editou um único cenário.

**Com isso, a Fase 7 está concluída.** A próxima entrega é a **Fase 9 — Endurecimento** (registro de auditoria, revisão de desempenho com volume real, LGPD e a pendência P-03). A **Fase 8 — Provedor de pagamento real continua bloqueada** por decisão que não é técnica: depende da **P-01** (escolher o provedor) e da **P-06** (confirmar as taxas), as duas com o dono do produto. As pendências **P-02** e **P-03** também seguem abertas.

- [x] Etapa 16 — **Fase 9, endurecimento**: até aqui o sistema fazia tudo o que precisava fazer, mas ninguém conseguia responder a três perguntas que aparecem justamente quando as coisas dão errado: *quem foi que fez isso?*, *isso continua rápido com gente de verdade usando?* e *o que acontece se alguém insistir na porta?* Esta etapa respondeu às três — e, num dos casos, a resposta foi "não precisa mexer em nada", o que é conclusão de medição, não falta de trabalho.

    **O registro de auditoria, que ninguém apaga pela aplicação.** Toda ação administrativa que mexe em vaga, em dinheiro ou em cadastro passou a deixar rastro na tabela `logs_auditoria`: quem fez, o que fez, sobre o quê, o motivo que a pessoa escreveu, de qual endereço, e quando. São sete tipos de ação registrados — cancelamento de inscrição, confirmação manual de pagamento, criação, alteração e remoção em qualquer cadastro, promoção de usuário e criação de conta administrativa. O ponto que dá valor a esse registro **não é a tabela, é a trava**: o próprio model recusa `update` e `delete`, sempre, lançando exceção. Não há opção, não há "modo administrador", não há exceção "só desta vez". A razão é simples de dizer e fácil de esquecer: **registro que pode ser corrigido depois não prova nada** — bastaria alguém apagar a própria linha para a auditoria virar enfeite. Combinar com a equipe "ninguém edita essa tabela" não sobrevive à primeira pessoa que escrever código novo sem ler o combinado; a trava no model sobrevive. (Quem tiver acesso direto ao banco continua podendo mexer na tabela por fora, e isso é proposital: a defesa nesse nível é permissão de banco de dados, não código PHP.)

    Duas outras regras acompanham o registro. A primeira: **auditoria é testemunha, não porteiro**. Se a gravação do log falhar, a ação administrativa já aconteceu e não pode ser desfeita por causa disso — o erro vai para o log da aplicação e a vida segue. O contrário transformaria o registro num ponto único de falha do painel inteiro. A segunda: **o log não guarda dado sensível**. Ele registra que o campo mudou, não o conteúdo do campo — nunca CPF, nunca impressão digital do documento, nunca senha, token ou código Pix inteiro. Há teste para as três coisas. E a tela de auditoria, no painel, é **somente leitura**, com filtro por período, por usuário e por ação, e exige a permissão `auditoria.ver`, que existe desde a Fase 6a restrita ao `administrador`: o organizador recebe 403.

    **A medição com 10.000 inscrições — e por que nada precisou mudar.** O sistema foi construído desde o começo com índices e consultas agregadas, com bom raciocínio por trás de cada escolha. Só que **raciocínio não é medição**: com as trinta inscrições do banco de demonstração, qualquer consulta parece instantânea, inclusive a que travaria a tela no dia do evento. Entrou um seeder de volume que cria um evento com **10.000 inscrições** coerentes — uma ordem de grandeza acima do evento real esperado —, e as cinco consultas mais pesadas do sistema foram medidas com o plano de execução do próprio banco na mão: os números do painel, a lista de inscrições com os filtros mais pesados combinados, a exportação em planilha, a página pública do evento e a varredura de inscrições vencidas.

    **O resultado: nenhum índice novo, nenhum cache.** Tudo o que uma pessoa espera olhando para a tela responde em dez milésimos de segundo ou menos. E isso é boa notícia, não preguiça — é a diferença entre "não mexemos" e "medimos e concluímos que mexer pioraria". **Todo índice custa na escrita**, e escrita é justamente o caminho da inscrição, o único que não pode ficar lento, porque é o que a pessoa faz enquanto disputa uma vaga com outras. Índice criado "por precaução" é custo garantido em troca de benefício imaginário. O cache tem raciocínio parecido: o painel responde em 5 milésimos de segundo, e um cache de 60 segundos existiria para economizar isso mostrando número velho ao organizador no dia em que ele mais precisa do número certo. O relatório inteiro, com o número de cada consulta e o motivo de cada decisão, está em **`docs/PERFORMANCE.md`** — inclusive as duas otimizações que *pareceriam* úteis e foram registradas como hipóteses não confirmadas, para quem um dia precisar delas.

    Apareceu **um número desconfortável**, e ele está escrito lá sem maquiagem: expirar 2.000 inscrições vencidas de uma só vez leva 18 segundos. Não há consulta lenta ali — são 18 mil idas e voltas ao banco, uma quantidade grande de operações rápidas. Corrigir exigiria fazer tudo em lote, e fazer em lote custaria a escrita condicional linha a linha (a garantia de que duas execuções simultâneas não devolvem a mesma vaga duas vezes) e o aviso de "prazo vencido" que hoje sai para cada pessoa. **Desempenho não paga esse preço.** A recomendação — limitar a varredura sob demanda a um teto por chamada — ficou registrada como pendência **P-10**, para virar decisão do dono do produto e plano próprio.

    **As portas expostas à internet.** O envio do formulário de inscrição ganhou limite por endereço de internet, em **dois níveis ao mesmo tempo e de propósito**: o do minuto é folgado, para não punir a família inteira saindo pela mesma conexão; o da hora é o que realmente segura um programa automatizado, porque nenhuma família manda sessenta inscrições numa hora e nenhum script consegue disfarçar isso esperando entre os pedidos. Quando o limite estoura, a pessoa lê **uma frase em português** dizendo o que aconteceu e quanto esperar, e não a página crua de erro do framework em inglês. O login do painel ganhou teto por endereço **por cima** do limite por e-mail que o Laravel já trazia — sem isso, quem varre uma lista de e-mails diferentes do mesmo lugar nunca esbarraria em limite nenhum. O aviso do provedor de pagamento ganhou um teto alto, sem tocar na regra de responder 200 a assinatura inválida (decisão D-18, intacta). E o limite de `POST /acesso`, que já existia, não foi mexido: o teste da resposta neutra continua verde.

    **Os cabeçalhos de segurança e a parte que mais podia dar errado.** Toda resposta passou a levar as instruções que o navegador obedece antes de qualquer código nosso rodar — não adivinhar tipo de arquivo, não deixar a página ser colocada dentro de um quadro de outro site, não vazar o endereço completo ao sair, e, em HTTPS, lembrar que este endereço só fala HTTPS. A peça central é a **Content-Security-Policy**, que diz de onde o navegador pode carregar cada coisa: se um dia alguém conseguir injetar um script numa tela, é ela que impede o script de rodar. Os scripts legítimos são liberados por **número de uso único sorteado a cada resposta** — script injetado por terceiro não tem como adivinhar o número da vez. **Não existe `unsafe-inline` em script-src**, que é o ponto de toda a defesa.

    Essa é exatamente o tipo de mudança que funciona na máquina de quem desenvolve e quebra depois do deploy, então ela foi **provada em navegador de verdade**: um cenário novo do Playwright abre a tela de pagamento com a CSP ligada e confere que o QR Code do Pix aparece, e abre a recuperação de acesso e confere que ela ainda funciona. Duas coisas quebraram durante a calibragem e foram consertadas com o entendimento correto, não afrouxando a regra: a tabela de rotas que o sistema escreve na própria página passou a receber o número de uso único, e o `style-src` recebeu `'unsafe-inline'` — uma concessão **declarada, e não descuido**, porque componente Vue escreve estilo direto no elemento (barra de progresso, altura calculada, cor de estado) e sem ela as telas quebrariam visualmente em produção. CSS injetado permite disfarce visual, o que é ruim; JavaScript injetado permite roubar sessão e enviar formulário no lugar da pessoa, o que é muito pior. A defesa fica onde o estrago é maior.

    **O teste de carga: 50 processos, 5 vagas.** O `ConcorrenciaTest` já provava desde a Fase 3, com seis processos de sistema operacional de verdade, que a capacidade não é furada. Agora são **cinquenta**, largando no mesmo instante, cada um com a sua própria conexão com o banco, disputando as **últimas cinco vagas** de uma atividade — a situação do dia em que o link cai no grupo da comunidade e todo mundo abre junto. **Cinco entraram, quarenta e cinco foram recusados na hora.** A conta de vagas terminou exatamente na capacidade, nem por uma a mais; nenhum processo travou esperando outro; e nenhuma vaga ficou presa por quem foi recusado. O tempo do caminho da inscrição sob essa disputa ficou entre **0,200 s e 0,455 s**, com mediana de **0,396 s** — e o que importa nesses números não é o valor, é a distância pequena entre o mais rápido e o mais lento: quando existe fila, essa distância cresce e o último espera o tempo de todos os anteriores somado. Ela não cresceu, o que confirma na prática a decisão de arquitetura de garantir vaga por gravação condicional em vez de tranca no banco.

    **As dependências.** `composer audit` e `npm audit` foram rodados: **nenhum aviso de segurança no PHP** e **zero vulnerabilidades no JavaScript** ao final. Uma correção foi aplicada no caminho — o `esbuild`, que é a ferramenta que empacota as telas, subiu de 0.24.2 para 0.25.12. É ferramenta de construção, não código que vai para o navegador da pessoa, e o empacotamento continua funcionando.

    **A revisão de superfície.** Confirmado que as rotas de simulação de pagamento continuam com a **dupla trava** da decisão D-29 (só nascem em `local`/`testing` com a chave ligada, e ainda passam por um middleware que confere as duas condições de novo), e que **nenhuma rota administrativa depende só de estar logado** — as onze rotas do grupo `/admin` exigem, além de login e e-mail confirmado, a permissão específica daquela tela. Ficou registrado no `docs/ARCHITECTURE.md` que **`APP_DEBUG` precisa ser `false` em produção**, que era a única das três verificações sem lugar escrito.

    **As contas.** **Suíte Pest: 452 testes, 2334 asserções** — 45 testes e 298 asserções a mais que ao final da Fase 7. **Suíte Playwright: 32 cenários** — os 28 anteriores intactos e verdes, mais 4 novos, todos da tela de auditoria e da CSP em navegador. Verificação de tipos com zero erros, `pint` e `lint` limpos, empacotamento das telas funcionando.

**Com isso, a Fase 9 está concluída** — e, com ela, **todo o escopo planejado está entregue, exceto a Fase 8 — provedor de pagamento real**, que continua bloqueada por decisão que não é técnica: depende da **P-01** (escolher o provedor) e da **P-06** (confirmar as taxas), as duas com o dono do produto.

**Uma coisa precisa ser dita sem rodeio, para ninguém supor o contrário: a revisão de LGPD NÃO foi feita.** Retenção de dados pessoais, prazo de descarte e anonimização **não existem** neste sistema hoje. Isso não foi esquecimento: implementar descarte de dado pessoal sob um prazo inventado seria decisão jurídica tomada por software. Depende da **P-04** (definir o prazo de retenção e descarte) e da **P-03** (o que fazer com pagamento reconhecido depois do prazo), as duas ainda abertas e as duas do dono do produto. Quando forem respondidas, a LGPD vira plano próprio. As pendências **P-02** (política de reembolso) e **P-10** (teto da varredura sob demanda) também seguem abertas.

- [x] Etapa 17 — **Fase 8a, o provedor de pagamento real (Efí)**: até aqui o sistema fazia tudo, menos a única coisa que justifica a existência dele — receber dinheiro. O provedor era simulado. Esta etapa trocou o simulado pela **Efí**, API Pix, e o critério de sucesso foi definido antes de escrever a primeira linha: **nenhuma regra de inscrição pode perceber a diferença**.

    **O que foi decidido antes de começar.** A escolha do provedor era a pendência **P-01**, aberta desde a Etapa 1, e o dono do produto a fechou em 2026-08-27: **Efí**. Junto com ela vieram oito decisões de recorte, tomadas em entrevista e registradas como a série **DA-16 a DA-24** na tabela de decisões. A mais consequente delas não tem a ver com pagamento nenhum: **toda leitura de credencial passa por um lugar só** (DA-24). Foi o que tornou a fase seguinte — cadastrar credenciais pela tela em vez do arquivo de ambiente — uma mudança no corpo de uma classe, em vez de uma reescrita.

    **A tese da fase, e como ela foi provada.** A promessa escrita em `docs/PAYMENTS.md` desde a Fase 4 era que trocar de provedor seria "escrever uma implementação do contrato e mudar uma linha de configuração". Promessa dessas é barata até o dia em que alguém tenta cumprir. Foi tentada, e cumpriu — com as provas na mão: **nenhum arquivo em `app/Actions/`, `app/Models/` ou `app/Enums/` foi alterado** nesta fase inteira (o `git diff` sobre essas três pastas volta vazio); as palavras `Efi` e `EfiPaymentGateway` **não aparecem** em nenhuma Action, Model, Job ou Service de inscrição; e **os 32 cenários de ponta a ponta passaram sem uma única edição** — a mesma tela, o mesmo QR Code, o mesmo caminho. Quem se inscreve não tem como notar que o dinheiro passou a ser de verdade, e é exatamente esse o objetivo.

    **O que a Efí impôs, e o que foi feito com cada imposição.** Integração com instituição financeira não é chamar um endereço e pronto — cada detalhe do fornecedor é uma decisão de desenho esperando para vazar para dentro do sistema. O identificador da cobrança tem que ter de 26 a 35 caracteres alfanuméricos, **sem hífen**: virou um ULID gerado por cobrança, e não derivado do código da inscrição, porque uma inscrição cancelada pode gerar uma segunda cobrança e reusar o identificador daria recusa por duplicidade. O valor viaja como texto decimal e o domínio guarda centavos inteiros: a conversão vive **só** na fronteira, sem número de ponto flutuante em ponto nenhum do caminho, com teste nos valores que costumam quebrar arredondamento. A Efí **não tem** situação de cobrança vencida — passado o prazo, ela continua respondendo "ativa" —, então quem decide que venceu continua sendo o prazo da inscrição, como sempre foi; traduzir "ativa" para "vencida" fecharia cobrança que a Efí ainda aceita pagar.

    **Duas colisões mudaram código que já existia, e as duas mereciam mudar.** A primeira: o aviso automático da Efí é uma **lista** de pagamentos, e um único aviso pode trazer vários. O contrato devolvia um evento só. Devolver apenas o primeiro perderia dinheiro **em silêncio** — ninguém reclamaria, e a segunda pessoa ficaria paga e sem vaga. O contrato passou a devolver uma lista e o controller desdobra o aviso em um registro e um trabalho por pagamento. O trabalho em segundo plano mudou **uma linha** e nenhuma regra: a idempotência de três camadas, a busca do pagamento e as Actions chamadas continuam idênticas. A segunda: o controller do aviso **citava o provedor simulado pelo nome** para saber onde ficava a assinatura. Parecia inofensivo enquanto só existia um provedor; a Efí manda a assinatura **no endereço**, não em cabeçalho, e o vazamento apareceu. Hoje quem recorta a requisição é o próprio provedor — o controller não sabe mais onde a assinatura viaja, e não deveria mesmo saber.

    **Uma distinção que vale dinheiro: assinatura inválida e falha nossa não são a mesma coisa.** Aviso com assinatura errada continua recebendo **200** (decisão D-18, intacta): responder 401 diria a quem tenta forjar avisos que ele acertou o endereço e errou só a assinatura. Mas **falha nossa** — banco fora do ar, erro inesperado — passou a **propagar** e virar erro 5xx, de propósito. A razão é concreta: a Efí reentrega o aviso até nove vezes ao longo de cerca de cinco horas quando não recebe sucesso. Responder 200 numa falha nossa jogaria fora, de graça, a única chance de receber aquele aviso de novo. As duas coisas têm teste.

    **O identificador da transferência, guardado antes de fazer falta.** O estorno ficou **fora** desta fase, de propósito: a política de reembolso (**P-02**) não foi decidida, e implementar devolução antes de existir regra de negócio é construir um botão que ninguém sabe quando apertar. Mas o `endToEndId` — o número que identifica a transferência no sistema bancário, e que **só chega no aviso** — passou a ser guardado desde já, em `pagamentos.metadados`, sem migração nova. Não guardar seria começar a fase de estorno com um passivo: todo pagamento anterior sem como devolver.

    **Como provar sem credencial, e por que isso exigiu um embrulho.** O SDK oficial da Efí usa cliente HTTP próprio, que as ferramentas de teste do Laravel **não** interceptam. Sem tratar isso, provar a emissão de cobrança exigiria credencial, certificado e rede — a suíte deixaria de rodar no computador de quem desenvolve. Por isso o SDK vive atrás de um embrulho fino, e é o embrulho que a suíte substitui por um duplo. São **36 testes novos** que rodam sem credencial, sem certificado e sem rede. Duplo, porém, prova desenho e não prova que a credencial funciona: para isso existe `php artisan efi:diagnostico`, que confere contra a homologação de verdade — certificado, token, cobrança, código Pix —, um passo por vez, e **só roda fora de produção**.

    **O que não foi feito, e não foi esquecimento.** Não existe ambiente publicado, então **nada foi ligado contra dinheiro de verdade**. O que a Efí exige do servidor — certificado da aplicação, cadeia de certificados da Efí no servidor web, HTTPS válido e o registro do endereço do aviso no painel — é **tarefa de implantação**, não de código, e virou roteiro passo a passo na seção 8.3 de `docs/ARCHITECTURE.md`, com a ordem de ligar e o que conferir em cada passo. Enquanto essas condições não estiverem certas, `PAYMENT_GATEWAY` continua em `fake`. Também **não** entraram: estorno (**P-02**), cobrança com vencimento (exige endereço completo de quem paga, que o formulário não coleta), split, cartão e a tela de cadastro de credenciais — esta última é a **Fase 8b**, e o terreno para ela foi preparado aqui.

    **Números da etapa:** **488 testes Pest com 3.455 asserções** (eram 452 com 2.334 antes da fase — +36 testes e +1.121 asserções), **32 cenários Playwright verdes sem nenhuma edição**, `vendor/bin/pint --test`, `npm run lint`, `npx vue-tsc --noEmit` e `composer audit` limpos. **Uma dependência nova**, o SDK oficial da Efí, e mais nenhuma. **Zero migrações.**

- [x] Etapa 18 — **Fase 8b, as credenciais da Efí pela interface**: a Fase 8a deixou o sistema capaz de cobrar de verdade, mas a credencial da instituição financeira morava no **arquivo de ambiente do servidor**. Isso tem dois custos que só aparecem depois: trocar uma chave exige alguém com acesso ao servidor, e arquivo de ambiente **não entra no backup do banco** — um contêiner recriado leva a configuração junto. Esta etapa tirou a credencial de lá e a colocou numa tela do painel. **A Fase 8 está fechada.**

    **A aposta da Fase 8a foi cobrada, e pagou.** A decisão **DA-24** dizia que toda leitura de configuração da Efí passaria por um lugar só, `ConfiguracaoEfi`, e que por isso trocar a fonte seria "mudar o corpo de uma classe". Cobrada aqui, a aposta pagou exatamente: `git diff --stat` sobre `app/Services/Payments/Efi/` mostra **um único arquivo alterado nesta fase inteira — `ConfiguracaoEfi.php`**. `EfiPaymentGateway`, `EfiClient` e `TraducaoDeStatus` não mudaram uma linha, e a suíte da 8a continuou verde sem edição. É o tipo de retorno que não aparece na fase em que a decisão é tomada, e sim duas fases depois.

    **A tela mais perigosa do sistema foi tratada como tal.** O que se digita ali decide para qual conta bancária vai o dinheiro do evento, e o desenho parte do princípio de que **a tela não pode virar um lugar de onde se lê a credencial**. Os cinco campos sigilosos — identificação, chave secreta, chave Pix, valor do aviso automático e **o conteúdo do certificado** — vão **cifrados** para o banco, pelo mesmo mecanismo que já protege o CPF (**D-08**); na linha crua do banco não há nada legível. **Nenhum valor guardado volta para o navegador**, nem mascarado pela metade: a tela recebe apenas um "existe um valor guardado". Disso decorre a regra que aparece em toda a tela — **campo em branco mantém o que está lá, nunca apaga** —, que não é conveniência: ninguém consegue redigitar o que a tela nunca mostrou. A permissão `pagamentos.credenciais` é **exclusiva do administrador**; quem organiza o evento não vê o item no menu e recebe **403** se digitar o endereço. E cada alteração vira registro em `logs_auditoria` dizendo **quais campos** mudaram — **nunca os valores**, nem antes nem depois.

    **Quem impede dois ambientes ativos é o PostgreSQL, não o PHP.** Homologação e produção são dois cadastros independentes (**DA-27**), e um indicador diz qual vale. A garantia de que só existe um ativo é um **índice único parcial** (`WHERE ativo = true`): tentar ativar o segundo devolve `SQLSTATE 23505` do banco, e não uma mensagem escrita por nós. É a mesma lição da **D-66** — verificação que vive só em PHP não sobrevive a duas requisições ao mesmo tempo.

    **O arquivo de ambiente virou reserva, e não sumiu** (**DA-26**). O cadastro da tela vence sempre; o arquivo só responde quando **não há nenhum ambiente ativo cadastrado**. Sem essa reserva, a suíte e a máquina de quem desenvolve precisariam de um banco semeado com segredo para rodar qualquer coisa. E a precedência **não se mistura**: o arquivo não completa o que falta num cadastro pela metade — um cadastro incompleto falha dizendo o que falta, em vez de cobrar com meia credencial da tela e meia do servidor.

    **Dois detalhes que evitam prejuízo silencioso.** O primeiro: salvar uma credencial **joga fora o token guardado** dos dois ambientes. Sem isso, o sistema continuaria falando com a Efí usando a credencial antiga por até uma hora depois da troca — e ninguém desconfiaria. O segundo: **passar para produção exige digitar a palavra `PRODUCAO`**, e a exigência é cobrada **no servidor**, não só na tela; clicar em "sim" já virou reflexo para quem usa computador, digitar não. A partir dali toda cobrança é dinheiro de verdade.

    **O certificado nunca toca o repositório.** O conteúdo mora cifrado no banco (**DA-25**) e só é escrito em disco — em `storage/certificados`, com permissão `0600`, pasta `0700` — no instante em que o SDK precisa dele. Esse arquivo é **cache descartável**: pode ser apagado a qualquer momento e o sistema o reescreve na chamada seguinte. O `.gitignore` cobre a pasta e as extensões `.p12`, `.pem` e `.pfx`, para que nem um arquivo solto fora dela escape — proteção que **já estava de pé desde a 8a** (`11e0a5d`) e que esta fase, ao contrário do que o plano previa, não precisou tocar. **Nenhum cenário de ponta a ponta envia certificado de propósito**: quem prova o caminho do upload é o Pest, que fabrica o arquivo em tempo de execução.

    **O que não foi feito, e não foi esquecimento.** Nenhum outro provedor entrou — a coluna `gateway` existe para o dia em que houver, mas a tela fala de Efí. Não há **rotação automática de credencial** nem **alerta de vencimento de certificado**: a tela mostra a data de validade quando o formato permite lê-la, e nada mais. **Estorno continua não existindo** (**P-02**). E, como na 8a, **nada foi ligado contra dinheiro de verdade**: não existe ambiente publicado.

    **Três rodadas, e vale registrar.** A execução desta fase morreu **duas vezes** no meio, por limite de ferramentas do agente, e foi retomada do zero de contexto nas duas. **Nada se perdeu, e o motivo é o processo, não a sorte:** o plano mandava commitar ao fim de cada passo, então cada retomada encontrou a árvore num estado provado e só precisou descobrir onde recomeçar. Duas consequências ficaram no histórico e não vale escondê-las: dois passos (o backend da tela e a própria tela) saíram **num commit só**, `3cd18f3`, porque a retomada não tinha como separá-los depois; e um efeito colateral do passo da permissão passou despercebido até a retomada seguinte — o teste que conta as permissões do seeder esperava nove, e passaram a ser dez. A contagem foi corrigida, **sem afrouxar nenhuma asserção**. A terceira rodada não escreveu código: fechou a documentação e o relatório.

    **Números da etapa:** **522 testes Pest com 3.661 asserções** (eram 488 com 3.455 — **+34 testes e +206 asserções**), **36 cenários Playwright verdes**, sendo os **32 anteriores sem nenhuma edição** mais os **4 novos** da tela de credenciais. `vendor/bin/pint --test`, `npm run lint`, `npx vue-tsc --noEmit` e `composer audit` limpos. **Nenhuma dependência nova.** **Uma migração** (`credenciais_pagamento`).

- [x] Etapa 19 — **Implantação: o sistema saiu da máquina de quem desenvolve.** Até aqui, todo o escopo de código estava pronto e **nada disso existia fora de um `docker-compose` de desenvolvimento**. Esta etapa empacotou a aplicação numa **imagem Docker** publicada no GHCR a cada push na `main` (**DA-31**) e entregou a **stack que o Portainer sobe** (`docker/compose.portainer.yaml`), atrás de um Traefik que já roda no servidor (**DA-33**). **Nenhuma regra de negócio foi alterada:** `git diff` sobre `app/` volta vazio, e os 36 cenários Playwright continuaram verdes sem uma edição.

    **A pendência mais antiga do projeto foi fechada, e não por lembrete: por desenho.** Desde a **Fase 7** este documento avisava, em três lugares diferentes, que **ninguém tinha subido o trabalhador da fila** — e que, sem ele, os cinco e-mails do participante ficavam parados, **sem erro na tela e sem ninguém reclamar**. A partir desta etapa o trabalhador é o **container `worker`**, rodando `queue:work redis --queue=emails` com reinício automático, e o agendador é o container `scheduler`, rodando `schedule:work`. São **três containers da mesma imagem** (**DA-32**), distinguidos só por `CONTAINER_ROLE` e pelo comando. A diferença entre "alguém precisa lembrar" e "o Docker levanta de novo" é a diferença entre uma pendência e um serviço.

    **O defeito que teria estragado em produção foi corrigido antes de existir produção.** Atrás do Traefik, o TLS termina na borda e o container recebe **HTTP simples**. Sem configurar proxy confiável, o framework acredita nisso — e **as URLs assinadas param de validar**: o link que o participante recebe por e-mail é gerado com `https` pelo trabalhador da fila e conferido numa requisição que o framework lê como `http`. A assinatura cobre o esquema, não bate, e **a pessoa recebe 403 no link que acabou de chegar**. Junto vinha o HSTS não emitido. A correção é `trustProxies` em `bootstrap/app.php` — **a única alteração em código de aplicação da etapa inteira** — e ela não ficou por conta de confiança: `tests/Feature/Producao/AtrasDeProxyTest.php` gera uma URL assinada em linha de comando e a confere numa requisição com `X-Forwarded-Proto: https`, exigindo também o HSTS na resposta. **O mesmo comportamento foi conferido dentro do container de verdade**, com contraprova: sem o cabeçalho do proxy, o HSTS não aparece.

    **O container faz sozinho as duas coisas que ninguém deve precisar lembrar.** `docker/entrypoint.sh` espera o banco **e o Redis** ficarem de pé, roda `php artisan optimize` e então, **só no papel `web`**, aplica as migrations e roda o `PapeisSeeder`. As duas restrições têm motivo concreto: migrations em um papel só porque os três containers sobem juntos e três processos migrando o mesmo banco é corrida garantida; e o seeder a cada boot porque ele é **idempotente por desenho** (**D-50**) e é ele que grava no banco as permissões que nasceram no código. Sem esse passo, **uma tela nova não aparece para ninguém** — some do menu, o acesso direto responde 403, e não há erro nenhum no log. Não é hipótese: aconteceu na Fase 8b, com a permissão `pagamentos.credenciais`.

    **Nada disso foi aceito por promessa.** A stack foi **subida de verdade na máquina de desenvolvimento**, com nome de projeto próprio, ao lado do ambiente de desenvolvimento que continuou rodando — os dois não colidem porque a stack de produção **não publica porta nenhuma no host**. Foram conferidos, com comando: os cinco containers de pé; as migrations aplicadas **uma vez só**, e no `app` (`aplicando migrations = 1` no `app`, `0` no `worker` e `0` no `scheduler`); o seeder idempotente em **dois boots** (2 papéis, 10 permissões e 16 vínculos nos dois, nenhuma linha duplicada); `/up` respondendo 200 com HSTS e a CSP da Fase 9 íntegra; `schedule:list` mostrando as três rotinas — e o log do scheduler mostrando uma delas **executando sozinha**; e o worker esvaziando a fila `emails` **sem tocar** na `default`. O registro completo, comando a comando, está em `.planning/feat/features/deploy-docker-portainer/prova-da-stack.md`.

    **E-mail de verdade, pela primeira vez.** Em produção o e-mail sai pela **Resend**, por API HTTPS (**DA-29**), e não por SMTP: as portas 25 e 587 costumam vir bloqueadas em servidor de nuvem, e a falha seria **silenciosa** — o trabalho ficaria na fila e ninguém receberia nada. É a única dependência nova da etapa. Em desenvolvimento nada muda: o e-mail continua parando no Mailpit.

    **O que ficou de fora, e é escolha, não esquecimento.** O **mTLS de verdade no aviso da Efí não foi implementado** (**DA-28**): a Efí recomenda exigir o certificado do cliente na borda, e aqui a defesa é dupla e diferente — o **HMAC** que a aplicação já confere em todo aviso mais uma **lista de IP no Traefik**, num router com prioridade maior, cobrindo **só** o endereço do webhook. A inscrição pública continua aberta a qualquer pessoa. Também ficaram fora: **backup automatizado** (e desde a 8b é o backup do banco que carrega a credencial da Efí — montar um `pg_dump` para fora da máquina é a primeira coisa a fazer depois de publicar), **monitoramento e alerta** (ninguém é avisado se o `worker` cair) e **réplica do banco**. Tudo isso está escrito na seção 13 de `docs/DEPLOY.md`, para ninguém supor o contrário.

    **A imagem tem 956 MB, e isso foi investigado, não aceito.** O plano mandava investigar qualquer coisa acima de ~600 MB. A conferência camada a camada mostrou que **825 MB são a imagem base oficial `dunglas/frankenphp:1-php8.4`** — exatamente a mesma que o `controle_tempo_ccc` já roda em produção neste servidor. Os **131 MB** restantes são o que este projeto acrescenta: extensões do PHP (51,6 MB), `vendor` de produção (46,2 MB), o código (4,6 MB) e os assets do Vite (0,9 MB). **Não entrou nada indevido** — `node_modules`, `vendor` de desenvolvimento, `.env`, `.git`, `tests/` e `.planning/` foram conferidos como ausentes de dentro da imagem. Encolher exigiria trocar a base pela variante Alpine, mudando a libc debaixo de uma aplicação já provada — não é decisão para o fim de uma fase de empacotamento, e fica registrada como possível melhoria.

    **Três rodadas de novo, e de novo nada se perdeu.** Como na Fase 8b, a execução morreu **duas vezes** por limite de chamadas do agente e foi retomada do zero de contexto. O motivo de nada ter se perdido continua sendo o processo: **commit ao fim de cada passo**, então cada retomada encontrou a árvore num estado provado e só precisou descobrir onde recomeçar. A terceira rodada não escreveu código — fechou a documentação e o relatório.

    **Números da etapa:** **533 testes Pest com 3.681 asserções** (eram 522 com 3.661 — **+11 testes e +20 asserções**, todos novos: 7 do proxy reverso e 4 do transporte de e-mail). **36 cenários Playwright verdes, sem uma linha editada.** `vendor/bin/pint --test`, `npm run lint`, `npx vue-tsc --noEmit`, `composer audit` e `npm audit` limpos. **Uma dependência nova** (`resend/resend-laravel`, **DA-29**). **Nenhuma migração.** **Nenhum arquivo em `app/` alterado.**

- [x] Etapa 20 — **A porta da rua deixou de ser a tela de exemplo do Laravel.** Até aqui, quem abrisse `inscricoes.cccista.com.br` encontraria a página do pacote inicial, com links para a documentação do framework — e o endereço do evento só chegava a quem já tivesse o link da vitrine no WhatsApp. Agora a raiz do domínio é uma **página própria** (**DA-34**) que responde três perguntas em segundos: **qual é o evento, quando ele é e como se inscrever**, com um botão grande que leva à vitrine.

    **A página está certa nos três estados, e cada um tem teste.** Com **um** evento aberto, ela o apresenta e encaminha. Com **mais de um**, destaca o de **início mais próximo** e lista os demais abaixo (**DA-38**) — nunca "o primeiro do banco", que é ordem sem critério e defeito esperando acontecer. Com **nenhum**, avisa com clareza e, **quando existe evento publicado com abertura futura**, diz qual é e a partir de quando: quem chegou cedo demais fica sabendo quando voltar (**DA-35**). O caso limite que engana está coberto por dois testes: evento **publicado, ou já com a situação "inscrições abertas", cuja janela ainda não começou**, aparece como *próximo* e **nunca com botão de inscrição** — oferecer um caminho que o servidor recusaria é pior do que não oferecer nada.

    **Nada de novo foi inventado, e isso é a decisão DA-37.** Sem logo, sem paleta, sem componente: a página usa o `PublicoLayout`, o botão, o alerta e a etiqueta que a vitrine já usava, e por isso parece da mesma família. **Nenhuma regra de negócio foi tocada** — `git diff` sobre `app/Actions/`, `app/Models/` e `app/Enums/` volta vazio, e quem sabe o que é "inscrições abertas" continua sendo o `Evento`, pelo scope `comInscricoesAbertas` e pelo método `inscricoesEstaoAbertas()`. A home **não reimplementou a regra**, que era o risco óbvio da fase.

    **A página mais acessada do sistema se monta com uma consulta.** Um `SELECT` só, com as colunas que a tela usa, trazendo de uma vez os eventos abertos e os que ainda vão abrir — e há teste que conta as consultas e falha se virarem duas. Nenhum dia, grupo ou atividade é carregado: a árvore do evento é assunto da vitrine. E **nenhum cache** (**D-79**): a medição da Fase 9 concluiu que cache sem número que o justifique só acrescenta uma fonte de verdade defasada.

    **O que a página leva para o navegador foi conferido na resposta, não no Resource.** Cada evento entregue tem exatamente dez chaves — nome, slug, resumo, as duas datas, o período escrito, situação e rótulo, se as inscrições estão abertas e quando abrem. **Nenhum id interno** (o slug é o identificador público), **nenhuma contagem de inscritos, nenhuma vaga restante, nenhum dado de participante**. Número de vaga na entrada vira pressão sem contexto e fica errado no segundo seguinte; ele continua sendo informação da vitrine.

    **Quem já se inscreveu tem por onde voltar (DA-36).** Um link discreto **"Já fiz minha inscrição"** leva a `/acesso`, já dizendo de qual evento se trata quando há um em destaque. Até aqui, quem perdesse o e-mail só chegava lá sabendo a URL de cor.

    **Números da etapa:** **542 testes Pest com 3.807 asserções** (eram 533 com 3.681 — **+9 testes e +126 asserções**). **40 cenários Playwright verdes**, sendo os **36 anteriores sem uma linha editada** mais os **4 novos** da home. `vendor/bin/pint --test`, `npm run lint`, `npx vue-tsc --noEmit` e `npm run build` limpos. **Nenhuma dependência nova, nenhuma migração, nenhum arquivo de domínio alterado.** O `Welcome.vue` do pacote inicial foi **removido**, e não guardado de lado: arquivo órfão de starter kit vira dúvida para quem chega depois.

- [x] Etapa 21 — **O Tailwind subiu de versão e o sistema trocou de cor.** Duas mudanças num plano só, e as duas mexem em tudo o que se vê: o **Tailwind passou de 3.4 para 4** e a paleta passou a ser a do **tema do shadcn studio** (**DA-39** e **DA-40**). **Nada foi redesenhado** — nenhuma tela mudou de layout, de espaçamento ou de hierarquia. O que mudou foi a base e as cores.

    **A consequência mais visível está escrita aqui para ninguém achar que quebrou:** o tema do studio é **azul**, e por isso o botão "Fazer inscrição" **deixou de ser vermelho**. A home, a vitrine, a tela de cobrança e o painel mudaram de cara. Foi decisão do dono do produto, reafirmada depois de a consequência ser exposta.

    **O `tailwind.config.js` foi removido.** Na versão 4 a configuração é o próprio CSS: `content` virou `@source`, `darkMode: ['class']` virou `@custom-variant`, `theme.extend` virou `@theme inline` e o `tailwindcss-animate` virou `tw-animate-css`. O PostCSS e o `autoprefixer` saíram do `vite.config.ts`, substituídos pelo plugin `@tailwindcss/vite` — a versão 4 resolve os prefixos sozinha. A tabela de equivalências está na **§14.3** do `docs/ARCHITECTURE.md`.

    **O risco desta etapa não era o erro que aparece — era a classe renomeada que some sem avisar.** A versão 4 renomeou dez utilitários, e uma classe que deixa de existir não dá erro: ela simplesmente não pinta nada. Os **187 arquivos `.vue`** foram varridos por cada uma das dez, e **153 substituições** foram feitas: `shadow-sm` → `shadow-xs` (7), `shadow` → `shadow-sm` (5), `rounded-sm` → `rounded-xs` (8) e **`outline-none` → `outline-hidden` (133)**. As outras seis não tinham nenhuma ocorrência. A última é a que mais assusta: `outline-none` some junto com o **anel de foco do teclado**, e foco visível é critério de acessibilidade — está em 133 lugares, todos conferidos.

    **Contraste foi a linha que não abriu (DA-42).** As quatro cores semânticas (`acao`, `sucesso`, `informacao`, `atencao`) **não existem no tema do studio** — ele tem `primary` e `destructive`, mas não tem "sucesso" nem "atenção". Elas foram **derivadas do esquema azul mantendo o significado** (**DA-41**), e **cada uma tem a razão de contraste calculada e escrita ao lado do valor**, no claro e no escuro, como o arquivo já fazia antes. **Três tons que o studio traz reprovariam e foram ajustados**: o cinza de texto secundário (4,39:1 sobre o próprio fundo cinza, escurecido até 4,79:1) e os textos que o studio põe por cima do azul e do vermelho no modo escuro (2,76:1 e 2,89:1, trocados por quase-preto, que rende 5,29:1 e 6,89:1). A decisão foi adotar o tema, não abrir mão de acessibilidade.

    **Uma armadilha nova, encontrada pelo próprio cenário de teste.** O tema do studio vem escrito em `oklch()`, e o cenário `home.spec.ts` mede contraste lendo a cor calculada do elemento e interpretando-a como `rgb()`. O navegador **não converte `oklch()`** — devolve como está. Com o tema em `oklch`, o cenário passou a medir os três números do oklch como se fossem canais de cor e acusou 2,55:1 num botão que tem 5,25:1. **O defeito era da migração, não do cenário**: as cores foram reescritas em hexadecimal, exatamente os mesmos tons, e a medição voltou a valer. O cenário não teve uma linha editada. Está registrado na §14.3 da arquitetura, porque é o tipo de coisa que morde de novo daqui a dois anos.

    **A prova.** **Os 40 cenários Playwright verdes, sem uma linha editada** — inclusive os três que medem contraste no navegador. **542 testes Pest com 3.807 asserções**, os mesmos de antes: `git diff` sobre `app/`, `routes/` e `database/` volta **vazio**, porque esta etapa não encostou no backend. `npm run build`, `npm run lint` e `npx vue-tsc --noEmit` limpos, **sem um aviso de classe desconhecida**. **A imagem Docker constrói** — o `npm ci` e o `npm run build` do estágio de assets rodaram na imagem, que é onde uma migração de build costuma quebrar depois de passar na máquina. E uma varredura de navegador percorreu as **oito telas principais em 360 px e em 1280 px, no claro e no escuro**, medindo **todo texto visível**: nenhum abaixo do mínimo da WCAG AA, e nenhuma tela escapando da largura.

    ✅ **O plano B foi feito na Etapa 22.** Quando esta etapa terminou, os 23 componentes ainda importavam de **`radix-vue`**, de propósito: a migração para **`reka-ui`** mexe em imports e componentes, esta mexeu em CSS, tokens e build — e, se um cenário quebrasse, era preciso saber qual das duas foi (**DA-43**). Nenhum arquivo passou a importar `reka-ui` aqui, e o gerador do shadcn **não foi rodado**.


- [x] Etapa 22 — **A troca da biblioteca de primitivas: `radix-vue` saiu, `reka-ui` entrou.** É o **plano B** anunciado na Etapa 21 (**DA-43**), e o que ele muda é o **comportamento** dos 23 componentes de interface — não a aparência. **Nenhuma tela mudou de layout, de espaçamento, de cor ou de hierarquia** (**DA-45**). `radix-vue` **saiu do `package.json`**: não há duas bibliotecas de primitivas no bundle, que é exatamente o que esta etapa existiu para evitar.

    **Renomear o import é a parte fácil e chata — 74 arquivos, mecânica.** O `npx vue-tsc --noEmit` fechou em **0 erros** logo depois, e é ele que prova que nenhum símbolo mudou de nome pelo caminho: `useForwardPropsEmits`, `useForwardProps`, `Primitive` e os tipos `*Props`/`*Emits` continuam existindo com o mesmo nome na versão 2. Nenhum `any`, nenhum `@ts-ignore`.

    **O perigo estava nas três mudanças que não dão erro de compilação.** Elas compilam, sobem, e param de funcionar na mão de quem usa:

    **A primeira: as variáveis CSS.** `--radix-*` virou `--reka-*`. Eram **quatro ocorrências em três arquivos** — a largura do menu do usuário, a altura e a largura do painel do menu de navegação, e a largura da lista do campo de seleção. Quando uma dessas fica para trás, **o elemento simplesmente perde a medida**: não há erro, não há aviso, e nenhum teste automatizado percebe. Foram trocadas e **conferidas no navegador de verdade**, lendo a variável calculada do elemento: no campo de seleção do formulário de inscrição, `--reka-select-trigger-width` devolve `361px` para um campo de `361px`, e a lista abre com `363px` (os 2 px da borda). A variável está sendo publicada pela versão 2 com o nome novo.

    **A segunda: `v-model:checked` virou `v-model`.** Eram dois pontos, e o primeiro é o mais grave do projeto inteiro: **o aceite dos termos** (`PassoRevisao.vue`), a última caixa antes de "Confirmar inscrição". Se essa conversão passasse batida, **ninguém conseguiria se inscrever** — e não haveria mensagem de erro nenhuma para explicar por quê. O outro é o "lembrar-me" do login administrativo. **O cenário `caminho-feliz.spec.ts` provou os dois lados**: rodado com o `build` velho, ele falhou exatamente no passo seguinte ao aceite; refeito o `build`, passou. A rede funciona.

    **A terceira: Presence.** Na versão 2, Accordion, Collapsible, Tabs e NavigationMenu passam a **montar o conteúdo mesmo quando ele está fechado**, e a visibilidade passa a depender do atributo `hidden`. O risco é conteúdo escondido aos olhos e **visível ao leitor de tela** — pior do que quebrado. **No projeto, nenhum desses quatro tem uso real:** não existe Accordion nem Tabs; o Collapsible existe em `components/ui/` mas **nenhuma tela o usa**; e o NavigationMenu, usado só no cabeçalho administrativo, aparece sem `NavigationMenuTrigger` e sem `NavigationMenuContent` — é uma barra de links, sem painel que abra. A varredura de navegador confirmou: nenhum conteúdo de Collapsible e nenhum viewport de NavigationMenu montado na página.

    **A porta do gerador foi destrancada (§14 do `docs/ARCHITECTURE.md`, reescrita).** As duas armadilhas do `components.json` deixaram de existir: o alias voltou a apontar para `resources/js/components` **minúsculo** — a que só mordia em Linux, que é onde a imagem Docker roda — e a chave `tailwind.config`, que apontava para um `tailwind.config.js` **removido na Etapa 21**, ficou vazia, como a ferramenta espera de um projeto em Tailwind 4. **O gerador não foi rodado** (**DA-44**): o objetivo era destrancar a porta, não passar por ela. Os 23 componentes foram **adaptados à mão**, porque vários carregam comentário em português e correção de acessibilidade que o gerador apagaria.

    **A prova.** **Os 40 cenários Playwright verdes, sem uma linha editada.** **542 testes Pest com 3.807 asserções**, os mesmos de antes: `git diff` sobre `app/`, `routes/` e `database/` volta **vazio** — esta etapa não encostou no backend. `npm run build`, `npm run lint` e `npx vue-tsc --noEmit` limpos, e **a imagem Docker constrói**, que é onde uma troca de dependência costuma quebrar depois de passar na máquina de quem escreveu.

    ⚠️ **Um defeito da Etapa 21 foi encontrado aqui, e não foi corrigido.** As classes `w-[--reka-...]` e `h-[--reka-...]` em `NavUser.vue` e `NavigationMenuViewport.vue` usam a forma curta do Tailwind 3, que **a versão 4 não suporta mais**: o CSS gerado sai `width:--reka-dropdown-menu-trigger-width`, **sem o `var()`**, e o navegador descarta a declaração. Medido no navegador: o gatilho tem `212,72px`, a variável publicada pelo `reka-ui` traz `212,71875px` corretamente, e o menu abre com `224px` — a largura do `min-w-56`, porque a largura pedida foi jogada fora. **Não é regressão desta etapa** — com o prefixo antigo o CSS saía igualmente inválido, e o `SelectContent.vue`, que já escrevia `var(...)`, funciona. Ficou de fora porque corrigir **mudaria a aparência de uma tela**, e DA-45 congelou a aparência. A correção é trocar `w-[--var]` por `w-(--var)`; deve entrar como tarefa própria, com o "antes e depois" à vista de quem decide.

    ✅ **Corrigido em 2026-08-28 pela D-86**, com o "antes e depois" à vista do dono do produto, como esta linha pedia. O alcance era maior do que se via daqui: não eram dois arquivos, eram **quatro**, e o pior deles não era o menu da conta — era a barra lateral do painel administrativo, que estava flutuando por cima do conteúdo.


## Em andamento

- [ ] **Colocar o sistema no ar — e nada do que falta é código.** Com a Etapa 19 fechada, a imagem, a stack e o roteiro existem. O que resta são **cinco ações humanas**, nesta ordem, todas descritas passo a passo em **`docs/DEPLOY.md`**:

    1. **Mesclar `feat/deploy-docker` na `main`.** É o push na `main` que faz o GitHub Actions construir e publicar a imagem no GHCR. Enquanto o ramo não for mesclado, **não existe imagem publicada** para a stack puxar (dá para antecipar com o disparo manual do workflow, em *Actions → Run workflow*). Confira uma vez, também, se *Settings → Actions → General → Workflow permissions* está em **Read and write** — sem isso o push do pacote é recusado.
    2. **Criar a stack no Portainer** a partir de `docker/compose.portainer.yaml`, preenchendo as variáveis de `env.docker.example`. **Guarde `APP_KEY` e `DOCUMENTO_HASH_PEPPER` no gerenciador de senhas antes de continuar**: a primeira cifra o CPF e a credencial da Efí, a segunda é o que detecta inscrição duplicada. Trocar qualquer uma delas depois do sistema em uso não tem comando de recuperação.
    3. **Criar o primeiro administrador**, por comando, dentro do container: `php artisan usuario:criar-administrador` (decisão **D-51**). **Não acontece sozinho e o sistema não avisa que faltou** — simplesmente não se entra em lugar nenhum.
    4. **Cadastrar a credencial da Efí pela tela** (`/admin/pagamentos/credenciais`), começando por homologação, e usar o **Testar conexão** antes de qualquer outra coisa. Na Efí, **marque os cinco escopos** da aplicação: `cob.write`, `cob.read`, `pix.read`, `webhook.write` e `webhook.read`. **A falta de `cob.write` já custou uma sessão inteira de diagnóstico neste projeto** — a autenticação funciona, o token é emitido, o "Testar conexão" passa, e só a emissão da cobrança falha, com um erro genérico. O passo a passo para quem não programa está na seção **10 de `docs/PAYMENTS.md`**.
    5. **Registrar o endereço do aviso no painel da Efí**, copiado pronto da própria tela, já com o `?hmac=` e terminando em `&ignorar=`.

    Enquanto isso não estiver feito, `PAYMENT_GATEWAY` continua em `fake`. **O detalhe que mais custa caro se passar despercebido:** sem o endereço do aviso registrado, **as cobranças nascem normalmente e nenhuma se confirma sozinha** — sem erro na tela, sem aviso e sem ninguém reclamar até alguém conferir o extrato. O sinal é a reconciliação confirmando *todos* os pagamentos cinco minutos depois; ela é a rede de segurança, não o caminho normal

- [ ] **Backup do banco — a primeira coisa a montar depois de publicar.** A stack não faz backup, e desde a Fase 8b é o **backup do banco que carrega a credencial da Efí** (**DA-25**). Um `pg_dump` agendado para fora da máquina é trabalho de infraestrutura, não de código, e não tem dono definido

- [ ] **A revisão de LGPD continua pendente e não foi feita.** Não há retenção, prazo de descarte nem anonimização de dado pessoal neste sistema. Ela ficou **fora da Fase 9 de propósito** (decisão D-76) e só pode começar depois da **P-04** e da **P-03**, as duas com o dono do produto

- [ ] **Duas decisões de negócio continuam com o dono do produto, e as duas seguram trabalho de código.** A **P-02** (política de reembolso) é o que falta para existir estorno: `refundPayment()` lança "não suportado" em voz alta de propósito (decisão **DA-18**), e o `endToEndId` que a devolução vai exigir **já está sendo guardado** desde a 8a, para que a decisão não chegue com um passivo de pagamentos impossíveis de devolver. A **P-06** (taxa efetiva, tarifas de conta e prazo de liquidação, confirmados com o comercial da Efí) não bloqueia a operação — nenhuma taxa entra em código ou configuração, por decisão **DA-23** —, mas bloqueia a precificação do evento

## Próximas tarefas

O que **está pronto** hoje: todo o núcleo transacional, todo o caminho do participante e a fundação do lado administrativo. Evento configurável, inscrição com reserva de vaga à prova de venda a mais, regras de seleção de atividades, cobrança Pix simulada, aviso automático (webhook) idempotente, expiração agendada que devolve vaga, reconciliação server-to-server e, pelo celular, a vitrine do evento, o formulário em quatro etapas, a tela de cobrança, o acompanhamento da inscrição e a recuperação do link de acesso. Do lado de dentro, quem administra entra por convite (papéis, permissões e conta criada por comando), tem o painel com os números do evento, cadastra o evento inteiro pela tela, encontra qualquer inscrição por filtro ou busca, leva a lista para planilha e age sobre um caso concreto — cancelando com motivo registrado ou, sendo administrador, reconhecendo um pagamento recebido por fora. Desde a Fase 7, quem se inscreve também é avisado por e-mail em cada passo — cobrança, lembrete de prazo, comprovante, prazo vencido e cancelamento —, tudo pela fila e sem que a mesma mensagem chegue duas vezes. Desde a Fase 9, toda ação administrativa que mexe em vaga, dinheiro ou cadastro deixa rastro que ninguém apaga pela aplicação; o painel e a lista foram medidos com 10.000 inscrições no banco e nada precisou ser otimizado (`docs/PERFORMANCE.md`); as portas expostas à internet têm limite de tentativas e cabeçalhos de segurança provados em navegador; e 50 processos disputando cinco vagas ao mesmo tempo não conseguem furar a capacidade. Desde a Fase 8a, a cobrança pode ser **de verdade**: `PAYMENT_GATEWAY=efi` faz o sistema cobrar pela Efí, com a mesma tela, o mesmo QR Code e a mesma confirmação automática — sem uma linha alterada em regra de inscrição. E desde a Fase 8b **quem administra configura isso sozinho, pela tela**: credencial, chave Pix, segredo do aviso e certificado da Efí são cadastrados no painel, para homologação e produção separadamente, cifrados no banco, sem que nada volte para o navegador e sem precisar de ninguém com acesso ao servidor. **Com isso, todo o escopo de código planejado está entregue.** E, desde a Etapa 19, ele **está empacotado**: uma imagem Docker publicada por GitHub Actions e uma stack de cinco containers para o Portainer, com o site, o trabalhador da fila e o agendador em processos separados, atrás do Traefik com HTTPS — **o trabalhador da fila deixou de ser uma pendência aberta desde a Fase 7**.

O que **não existe** — e é bom que esteja escrito, porque nada disso se resolve escrevendo código:

- **Dinheiro circulando de fato.** A imagem e a stack existem, mas **ninguém as subiu num servidor ainda**. Falta mesclar em `main` para publicar a imagem, criar a stack no Portainer, criar o primeiro administrador por comando, cadastrar a credencial real pela tela e registrar o endereço do aviso na Efí. Nenhuma dessas cinco é tarefa de código, e todas estão em `docs/DEPLOY.md`.
- **Estorno.** Depende da política de reembolso (**P-02**), que continua com o dono do produto. Cancelar inscrição já confirmada **não devolve dinheiro** hoje (decisão D-56).
- **A taxa efetiva confirmada por escrito** com o comercial da Efí (**P-06**) — bloqueia a precificação do evento, não a operação.
- **Qualquer coisa de LGPD. A revisão nunca foi feita:** não há política de retenção, não há prazo de descarte e não há anonimização de dado pessoal neste sistema. Ficou fora da Fase 9 **de propósito** (decisão D-76), porque descarte de dado pessoal é decisão jurídica, e depende da **P-04** e da **P-03**. É o maior buraco conhecido do projeto e vira plano próprio no dia em que as duas forem respondidas.
- **Backup, monitoramento e réplica do banco.** A stack não tem nenhum dos três. Ninguém é avisado se o `worker` cair, e os volumes do PostgreSQL vivem no servidor e mais nada — o que importa em dobro porque, desde a Fase 8b, é o backup do banco que carrega a credencial da Efí.
- **mTLS de verdade no aviso da Efí** (**DA-28**). A defesa é HMAC mais lista de IP no Traefik. É um desvio consciente do que a Efí recomenda, escrito como tal em `docs/ARCHITECTURE.md` §13.4 e em `docs/DEPLOY.md` §8.2.

O que **falta**, por fase:

### Fase 5a — Site público ✅ concluída
- [x] Vitrine `/eventos/{slug}`, formulário de inscrição em quatro etapas e tela de cobrança Pix
- [x] Controllers de leitura (`EventoPublicoController`, `InscricaoPublicaController`, `PagamentoController`) e Resources sem vazamento de dado pessoal
- [x] Volta à cobrança por **URL assinada com validade** (pendência **P-05** resolvida: decisão DA-05)
- [x] Regras de seleção espelhadas no navegador (`useSelecaoAtividades`), sempre como conforto — quem decide continua sendo o servidor
- [x] Doze cenários de ponta a ponta com Playwright, contra banco semeado do zero

### Fase 5b — Área do participante ✅ concluída
- [x] Página de acompanhamento da inscrição por link assinado, com linha do tempo derivada dos carimbos de tempo que o domínio já grava e histórico completo da cobrança
- [x] Recuperação do link de acesso por e-mail em `/acesso`, com resposta sempre neutra e link válido por sete dias
- [x] Segunda via do Pix sob demanda, enquanto o prazo não venceu
- [x] Nove cenários novos de ponta a ponta com Playwright, sem tocar nos 12 da Fase 5a
- [ ] Cancelamento da própria inscrição pelo participante — **continua fora do escopo** (decisão D-45). O dono do produto ainda não decidiu se quer oferecê-lo; enquanto não decidir, quem precisa desistir fala com a organização, que cancela pelo painel da Fase 6

### Fase 6a — Acesso administrativo e painel ✅ concluída
- [x] Papéis (`administrador`, `organizador`) e nove permissões em português, com seeder idempotente (decisão D-50)
- [x] Cadastro público fechado; conta administrativa por `php artisan usuario:criar-administrador` (decisão D-51)
- [x] Grupo de rotas `/admin` com autenticação, e-mail confirmado e permissão obrigatória em cada rota
- [x] Painel: inscrições por situação, vagas restantes por atividade e dinheiro recebido/pendente, por evento, tudo em consulta agregada
- [x] Pendência **P-09** fechada: `vue-tsc --noEmit` roda com zero erros (decisão D-52)
- [x] Quatro cenários novos de ponta a ponta, sem tocar nos 21 anteriores

### Fase 6b — Cadastros e gestão de inscrições ✅ concluída
- [x] CRUDs de evento, dias, grupos de atividades, atividades, conflitos, cidades e grupos de participantes, com cada restrição do banco espelhada em mensagem amigável antes de o PostgreSQL recusar
- [x] Lista de inscrições com filtros combináveis, busca por nome, e-mail e código público — **nunca por pedaço de CPF** — e paginação que preserva o filtro
- [x] Exportação em CSV respeitando os filtros da tela, em fluxo, com `;` e BOM UTF-8 e sem CPF
- [x] Ficha da inscrição com o histórico completo da cobrança
- [x] Cancelamento administrativo de inscrição (permissão `inscricoes.cancelar`), com motivo obrigatório, devolução de vaga na ordem canônica e teste de concorrência real (decisões D-55, D-56 e D-61)
- [x] Confirmação manual de pagamento (permissão `pagamentos.confirmar-manual`, restrita ao administrador), com observação obrigatória e origem manual registrada, sem identificador de provedor forjado (decisões D-55, D-57, D-59 e D-60)
- [x] Três cenários novos de ponta a ponta, sem tocar nos 25 anteriores

**Com isso, a Fase 6 está concluída** (6a e 6b).

### Fase 7 — Comunicação ✅ concluída
- [x] Ouvintes de `InscricaoCriada`, `InscricaoConfirmada`, `InscricaoExpirada` e `InscricaoCancelada` — os quatro anúncios do domínio finalmente têm quem os escute, e **nenhuma regra de inscrição ou de pagamento mudou** para isso (**a decisão D-12 está encerrada**)
- [x] Cinco e-mails em português simples, com versão em texto puro além do HTML, sempre com link assinado e **nunca** com CPF, telefone, impressão digital ou código Pix inteiro (decisões D-65 e D-68)
- [x] Lembrete de prazo (`inscricoes:lembrar-prazo`, a cada 15 minutos, janela padrão de 24 horas, em lotes com `chunkById`)
- [x] Tabela `comunicacoes_enviadas` com unicidade `(inscricao_id, tipo, canal)`: **é o banco, e não uma verificação em PHP, que impede a segunda cópia** (decisão D-66)
- [x] Tudo na fila `emails`, com 3 tentativas e espera de 1, 5 e 15 minutos; falha definitiva vai para `failed_jobs` e não afeta inscrição, vaga nem pagamento (decisões D-67 e D-69)
- [x] **A pendência de infraestrutura está fechada desde a Etapa 19:** o trabalhador da fila é o container **`worker`** da stack de produção, rodando `queue:work redis --queue=emails` com reinício automático, e o agendador é o container `scheduler`. Foi provado consumindo a fila `emails` — e **sem tocar** na `default`. Ficou aberto de propósito da Fase 7 até aqui, e este era o item que mais tempo passou avisando em silêncio
- [ ] WhatsApp continua **fora do escopo**: existe apenas a coluna `canal` e o vocabulário de tipos de mensagem, sem contrato, adaptador nem configuração (decisão D-70)

### Fase 8a — Provedor de pagamento real (Efí) ✅ concluída
- [x] Provedor escolhido: **Efí**, API Pix (decisão **DA-16**) — **a pendência P-01 está fechada**
- [x] `EfiPaymentGateway` cumprindo o mesmo contrato `PaymentGateway`, registrado no `match` do `PaymentServiceProvider`, com o SDK oficial isolado atrás de um embrulho fino (decisão **DA-21**)
- [x] **Nenhuma Action, Model ou Enum de domínio alterado** — o `git diff` sobre `app/Actions/`, `app/Models/` e `app/Enums/` volta vazio, e `SituacaoPagamento::deStatusExterno()` não foi tocado: o vocabulário neutro dele já dava conta do da Efí
- [x] Aviso automático real: assinatura conferida em tempo constante, aviso em **lote** desdobrado em um efeito por pagamento (decisão **DA-17**), endereço aceito com e sem o sufixo que a Efí acrescenta, e o `endToEndId` guardado (decisão **DA-18**)
- [x] Toda a configuração atrás de um lugar só (decisão **DA-24**), com teste que falha se um segundo arquivo passar a lê-la
- [x] `php artisan efi:diagnostico` para provar à mão contra a homologação, travado fora de `local`/`testing` (decisão **DA-20**)
- [x] Roteiro do que a Efí exige do servidor na seção 8.3 de `docs/ARCHITECTURE.md` (decisão **DA-19**)
- [x] **488 testes Pest** e **32 cenários Playwright sem edição** — a prova de que a troca é invisível para quem se inscreve
- [ ] **Não foi ligado contra dinheiro de verdade**: não há ambiente publicado. As tarefas de servidor são de implantação, não de código

### Fase 8b — Credenciais da Efí pela interface ✅ concluída
- [x] Tabela `credenciais_pagamento` com os **cinco campos sigilosos cifrados em repouso** (decisão **DA-25**), `CHECK` no ambiente, unicidade `(gateway, ambiente)` e **índice único parcial** garantindo um ativo por gateway — provado com a exceção `SQLSTATE 23505` do PostgreSQL, não com verificação em PHP
- [x] Fonte da configuração trocada do arquivo de ambiente para o **banco**, dentro de `ConfiguracaoEfi` — e **só ali**: `git diff --stat` sobre `app/Services/Payments/Efi/` mostra **um único arquivo alterado** na fase inteira (a decisão **DA-24** cobrada e paga)
- [x] Arquivo de ambiente mantido como **reserva** (decisão **DA-26**), usado só quando não há ambiente ativo cadastrado — e sem completar o que falta num cadastro pela metade
- [x] Dois ambientes independentes, **homologação e produção**, com troca pela tela e **confirmação escrita exigida no servidor** para passar a cobrar de verdade (decisão **DA-27**)
- [x] Tela `Admin/Pagamentos/Credenciais/Index.vue` com os dois blocos, gerador do valor do aviso automático, endereço do webhook pronto para copiar (com `?hmac=` e `?ignorar=`), envio do certificado e teste de conexão que reaproveita a lógica do `efi:diagnostico` — **sem emitir cobrança**
- [x] **Nenhum segredo volta para o navegador**, nem mascarado: campo em branco **mantém** o valor guardado e nunca apaga
- [x] Permissão própria `pagamentos.credenciais`, **só do administrador** (**D-55**), no seeder idempotente — **o organizador recebe 403** e não vê o item no menu
- [x] `AcaoAuditada::AlterouCredencialPagamento`: alteração e troca de ambiente registradas em `logs_auditoria` com **quais campos** mudaram, **nunca os valores** — teste dedicado prova o vazamento zero, e falha de auditoria não desfaz a gravação
- [x] Certificado cifrado no banco e materializado em `storage/certificados` com permissão `0600` só no uso; `.gitignore` cobrindo a pasta e as extensões `.p12`, `.pem` e `.pfx`
- [x] **522 testes Pest (3.661 asserções)** e **36 cenários Playwright** — os **32 anteriores sem edição** mais 4 novos da tela
- [ ] Definir a política de reembolso (**P-02**) e ligar `refundPayment()` a uma Action de estorno — hoje ele lança "não suportado" em voz alta (decisão **DA-18**), e o `endToEndId` de que ele vai precisar **já está sendo guardado**
- [ ] **Rotação automática de credencial e alerta de vencimento do certificado ficaram fora do escopo.** A tela mostra a validade quando o formato permite lê-la, e nada mais: no dia em que o certificado vencer, toda cobrança nova para de funcionar de uma vez
- [ ] **Nenhum outro provedor além da Efí.** A coluna `gateway` existe para o futuro, mas a tela fala de Efí

### Fase 9 — Endurecimento ✅ concluída
- [x] Tabela `logs_auditoria` **append-only de verdade**: o model recusa `update` e `delete`, sempre, e há teste que exige a exceção (decisões D-77 e D-78)
- [x] Sete ações administrativas registradas — cancelamento, confirmação manual de pagamento, criação/alteração/remoção em cadastro, promoção de usuário e criação de conta administrativa
- [x] Tela de auditoria somente leitura, com filtro por período, usuário e ação, exigindo `auditoria.ver` (o organizador recebe 403)
- [x] Medição com **10.000 inscrições** no banco: as cinco consultas de maior peso, com o plano de execução do banco registrado em `docs/PERFORMANCE.md`
- [x] **Nenhum índice novo e nenhum cache** — a medição concluiu que mexer pioraria, e o motivo está escrito (decisão D-79)
- [x] Limite de requisição em `POST /inscricoes` (dois níveis, com recusa em português), no login administrativo e no webhook, sem tocar na D-18 nem na D-48 (decisões D-80 e D-81)
- [x] Cabeçalhos de segurança em toda resposta e CSP por número de uso único, **sem `unsafe-inline` em script-src**, provada em navegador com Playwright (decisões D-82 e D-83)
- [x] Teste de carga com **50 processos disputando 5 vagas**: capacidade nunca furada, sem impasse, tempos registrados em `docs/PERFORMANCE.md` (decisão D-84)
- [x] `composer audit` sem aviso e `npm audit` com zero vulnerabilidades; `esbuild` atualizado (decisão D-85)
- [x] Revisão de superfície: dupla trava das rotas de simulação (D-29) intacta, nenhuma rota administrativa só com login, e `APP_DEBUG=false` para produção documentado em `docs/ARCHITECTURE.md`
- [ ] **Revisão de LGPD — NÃO foi feita e continua pendente.** Retenção, prazo de descarte e anonimização não existem neste sistema. Ficou fora desta fase de propósito (decisão D-76) e depende da **P-04** e da **P-03**
- [ ] Decidir o que fazer com pagamento reconhecido depois do prazo (**P-03**) — ver a decisão D-34
- [ ] Teto da varredura sob demanda de inscrições vencidas (**P-10**) — recomendação registrada na seção 4.6 de `docs/PERFORMANCE.md`, não implementada porque mudaria regra de negócio

### Implantação — imagem Docker e stack no Portainer ✅ concluída

Não era uma fase do plano original de código: é o que faltava para o sistema **existir fora da máquina de quem desenvolve**.

- [x] `Dockerfile` de **três estágios** (assets do Vite → `vendor` sem dependências de desenvolvimento → FrankenPHP com PHP 8.4 e as extensões, **incluindo `redis`**) e `.dockerignore` — conferido de dentro da imagem: sem `node_modules`, sem `vendor` de desenvolvimento, sem `.env`, sem `.git`, sem `tests/` e sem `.planning/`
- [x] `docker/entrypoint.sh` com **papéis**: espera o banco **e o Redis**, roda `optimize` e aplica `migrate --force` e o `PapeisSeeder` **só no papel `web`** (§3.6 do plano) — provado com os três containers subindo juntos
- [x] `docker/compose.portainer.yaml` com os cinco serviços, volumes nomeados e **nenhuma porta publicada no host**; `APP_ENV=production` e `APP_DEBUG=false` **fixos**, não variáveis
- [x] **`app`, `worker` e `scheduler` da mesma imagem** (decisão **DA-32**), distinguidos por `CONTAINER_ROLE` e pelo comando; o `scheduler` com **uma réplica, sempre**
- [x] **`trustProxies` em `bootstrap/app.php`** — a única alteração em código de aplicação —, com teste provando que uma URL assinada gerada em linha de comando valida numa requisição com `X-Forwarded-Proto: https` e que o HSTS é emitido
- [x] Router próprio do Traefik para o webhook, cobrindo `/webhooks/pagamentos` **e** `/webhooks/pagamentos/pix`, com **prioridade maior** que o router do site e middleware de **lista de IP** só nele (decisão **DA-28**)
- [x] `.github/workflows/docker-publish.yml` publicando no **GHCR** a cada push na `main` (decisão **DA-31**)
- [x] E-mail de produção pela **Resend**, por API HTTPS (decisão **DA-29**) — a única dependência nova
- [x] `env.docker.example` com todas as variáveis do stack e **nenhum valor real**, e `docs/DEPLOY.md` com o roteiro inteiro, do primeiro administrador ao registro do webhook
- [x] Stack **subida de verdade** na máquina de desenvolvimento e cada critério conferido com comando — o registro está em `.planning/feat/features/deploy-docker-portainer/prova-da-stack.md`
- [ ] **Ninguém subiu isso num servidor ainda.** As cinco ações humanas que faltam estão em "Em andamento"
- [ ] **mTLS de verdade no webhook ficou fora** (**DA-28**) — desvio consciente do que a Efí recomenda
- [ ] **Backup, monitoramento e réplica do banco ficaram fora**, e não têm dono definido
- [ ] **O middleware `ipallowlist` do Traefik não foi exercitado de verdade**: exigiria um Traefik com o certificado do domínio, que não existe na máquina de desenvolvimento. O que se provou foram as labels e o desenho — o mesmo arranjo que o `controle_tempo_ccc` já roda em produção. A conferência definitiva é o primeiro aviso da Efí chegar

---

### Correção — a barra lateral administrativa por cima do conteúdo ✅ concluída

Não é uma fase do plano: é um defeito de interface que atravessou duas etapas e **44 cenários de navegador** sem ser visto por ninguém.

- [x] **Dez ocorrências** da sintaxe de variável CSS do Tailwind 3 (`w-[--x]`) trocadas pela do Tailwind 4 (`w-(--x)`) em **quatro** arquivos — `Sidebar.vue` (seis), `SidebarMenuSkeleton.vue`, `NavUser.vue` e `NavigationMenuViewport.vue`. O CSS construído saiu de **seis declarações inválidas para zero** (decisão **D-86**)
- [x] A barra lateral do painel voltou a ter os **256px** que o `SIDEBAR_WIDTH` declara, o conteúdo voltou para o lado dela em vez de ficar por baixo, e o **botão de recolher voltou a ser alcançável** — ele nunca tinha sumido, estava coberto pela própria barra
- [x] A gaveta do celular voltou aos **288px** de antes da Etapa 21. Precisou de um `!` na classe, e o porquê está escrito ao lado dele no `Sidebar.vue` — é a pendência **P-11**
- [x] **Duas provas novas, e as duas foram vistas vermelhas antes de ficarem verdes:** `tests/e2e/admin-barra-lateral.spec.ts`, o primeiro cenário da suíte em **tela grande** (1280×800), e `tests/Feature/Interface/CssConstruidoTest.php`, que lê o CSS que o `npm run build` produziu e recusa declaração com valor de variável solto
- [x] **O passo de testes do CI foi consertado no mesmo commit.** Ele chamava `./vendor/bin/phpunit`, que neste projeto morre antes do primeiro teste
- [ ] `tailwind-merge` continua na **2.6.0**, que não entende a sintaxe nova — pendência **P-11**
- [ ] `npm run format:check` reprova **52 arquivos**, e reprovava os mesmos 52 antes desta correção — pendência **P-12**

### Identidade visual do lado público (plano 1 de 3) ✅ concluída

Não é uma fase do plano original: é a adoção da identidade do `ccc-redesign.html`
no lado do visitante (**DA-51**). **Nenhuma tela mudou de conteúdo, de estrutura
ou de texto** — o que mudou foi como o que já existe se parece.

- [x] Tokens da identidade escopados em `[data-tema='publico']`, **com a razão de contraste calculada e escrita ao lado de cada tom**, mais a variante escura derivada (**DA-54**)
- [x] O escopo mora no **`<html>`**, escrito pelo Blade (primeira pintura) e pelo `app.ts` (toda navegação do Inertia) — **DA-52**
- [x] Bricolage Grotesque nos títulos e DM Mono nos números, as duas pelo **`fonts.bunny.net`**: a CSP **não mudou** e o cenário `seguranca-csp.spec.ts` não foi tocado
- [x] Botão pílula de 48px e as quatro etiquetas do protótipo mapeadas nas variantes que **já existiam** — nenhuma variante nova, nenhum componente duplicado (**DA-56**)
- [x] `TrilhaDeDias.vue`, o elemento-assinatura: a linha pontilhada que liga os dias. A linha e os pontos são decoração e **saem do leitor de tela**; as datas ficam. Quem o coloca em tela é o plano 2
- [x] **Três tons do protótipo reprovaram em AA e entraram escurecidos** (**DA-55**), com o valor original registrado no comentário
- [x] **Duas provas novas, e as duas foram vistas vermelhas antes de ficarem verdes:** `tests/Feature/Interface/TemaPublicoTest.php`, que **recalcula** a razão de cada par lendo o `app.css` em vez de confiar no comentário, e `tests/e2e/identidade-publica.spec.ts`, cujo terceiro cenário abre a lista de cidades **teleportada para fora da página** e exige que ela saia no tema público
- [x] **Nenhuma tela administrativa mudou de cor**, conferido por token no navegador em `/admin/painel` e `/admin/inscricoes` e à vista, nos dois tamanhos
- [x] **550 testes Pest** e **51 cenários de navegador** passando, sem nenhum dos 47 antigos editado

---

## Decisões

> A série **DA-16 a DA-24** foi tomada em entrevista com o dono do produto em 2026-08-27, antes de a Fase 8a começar, e recebeu prefixo próprio por ser o bloco de decisões do provedor de pagamento real. A série **DA-25 a DA-27**, do mesmo prefixo, foi tomada antes da Fase 8b e trata de **onde a credencial mora**. A série **DA-28 a DA-33**, também do mesmo prefixo, foi tomada antes da etapa de implantação e trata de **como o sistema é publicado**. As decisões **D-86, D-86.1 e D-87** voltaram ao prefixo simples: são de 2026-08-28 e saíram da correção da barra lateral administrativa.

| # | Decisão | Motivo |
|---|---------|--------|
| D-01 | O domínio (banco, modelos, situações, regras) é escrito em **português, sem acento nem cedilha** | Quem discute as regras do evento fala português. Acento em nome de coluna obrigaria aspas em toda consulta no PostgreSQL |
| D-02 | A estrutura do framework permanece em **inglês** (`app/Http/Controllers`, `app/Jobs`, `created_at`) | É a convenção da ferramenta; mudar quebraria comportamentos automáticos |
| D-03 | A inscrição **não** tem estado "pago" | "Pago" é fato do dinheiro e pertence ao pagamento. Duas fontes da verdade discordariam em caso de estorno |
| D-04 | Reserva de vaga por **contador atômico** (compare-and-swap), não por trava na linha do evento | Evita fila única em eventos de alta procura, com a mesma garantia contra venda a mais |
| D-05 | Ordem de reserva sempre **evento → atividades em ordem crescente de id** | Ordem fixa elimina a possibilidade de duas inscrições travarem uma esperando a outra |
| D-06 | Dinheiro sempre em **centavos, número inteiro** | Número decimal aproximado gera erro de arredondamento em dinheiro |
| D-07 | **Sem tabela de aceite de termos** no MVP: dois campos na inscrição bastam | Menos superfície para o mesmo requisito. Se um dia houver mais de um termo por inscrição, vira tabela |
| D-08 | CPF **cifrado** no banco + **impressão digital** separada para duplicidade | Permite bloquear inscrição duplicada sem manter o número legível |
| D-09 | Cidades e grupos de participantes são **catálogo global**, não pertencem a um evento | Simplifica o cadastro. Se o negócio exigir cidades por evento, é mudança de escopo |
| D-10 | Fuso: aplicação em `America/Sao_Paulo`, banco em UTC, colunas com fuso embutido | Evita erro de horário de verão e de servidor em outro fuso |
| D-11 | `lista_espera` existe na lista de situações mas **não é alcançável** nesta entrega | Reserva o valor para a fase pós-MVP sem criar caminho de código morto |
| D-12 | ~~Nenhum e-mail nesta entrega: apenas os anúncios internos (eventos de domínio) sem ouvintes~~ — **ENCERRADA na Fase 7**: os quatro anúncios (`InscricaoCriada`, `InscricaoConfirmada`, `InscricaoExpirada` e `InscricaoCancelada`) passaram a ter ouvinte, e cada um faz sair exatamente um e-mail | A aposta feita lá atrás se confirmou: **os ouvintes entraram sem tocar em nenhuma regra de inscrição ou de pagamento** — nenhuma Action, nenhum Enum de domínio, nenhuma migração existente, e nenhum anúncio mudou de lugar ou de momento. Foi possível porque os anúncios já eram disparados depois que a transação fecha e só na chamada que de fato mudou a situação (D-32). Encerrada, e mantida aqui porque explica por que os eventos existiam sem ouvinte durante quatro fases |
| D-13 | Situações gravadas como **texto** no banco, controladas por Enum do PHP, em vez do tipo `enum` do PostgreSQL | Acrescentar um valor ao tipo `enum` do PostgreSQL é alteração de esquema com restrições. A lista de situações vai crescer (`lista_espera`) |
| D-14 | O par de `conflitos_atividades` é **normalizado** (`atividade_a_id < atividade_b_id`), garantido por restrição do banco | Sem isso, (7,3) e (3,7) seriam duas linhas para o mesmo conflito e a unicidade não protegeria nada |
| D-15 | Unicidade de e-mail e CPF é **parcial** (só vale para inscrições ativas) | Uma unicidade comum bloquearia para sempre; o participante precisa poder tentar de novo depois da expiração |
| D-16 | Em RN-04, grupo **opcional** com mínimo maior que zero significa "ou nada, ou pelo menos o mínimo" | Interpretação mais restritiva entre as possíveis, conforme a política de ambiguidade do plano |
| D-17 | O provedor de pagamento devolve `parseWebhook` (tradução), não `handleWebhook` (ação) | Mantém a decisão sobre o domínio de um lado só da fronteira; trocar de provedor não muda o efeito na inscrição |
| D-18 | O endereço de webhook responde **200 mesmo com assinatura inválida**, gravando o aviso como inválido e sem produzir efeito | Responder 401 informaria a quem tenta forjar avisos que ele acertou o endereço e errou só a assinatura |
| D-19 | O PostgreSQL do Sail é publicado na porta **55432** do computador (variável `FORWARD_PGSQL_PORT`), e não na 5432 | A porta 5432 já estava ocupada por outro PostgreSQL instalado na máquina de desenvolvimento, que respondia no lugar do contêiner. A porta interna do contêiner continua sendo 5432 |
| D-20 | Os testes rodam no banco `testing` do mesmo PostgreSQL, com `DB_HOST`/`DB_PORT` fixados em `phpunit.xml` | Restrição parcial de unicidade, `CHECK`, `jsonb` e concorrência real só têm valor testados no mesmo motor de produção |
| D-21 | As recusas de inscrição partem de uma classe base comum (`InscricaoInvalidaException`), e RN-01/RN-02 ganharam a sua própria (`InscricaoIndisponivelException`), além das três previstas no plano | A classe base guarda as mensagens já agrupadas pelo campo do formulário, e o controller as devolve como erro de validação (422). Sem ela, cada recusa teria o seu próprio formato e o controller decidiria texto para o participante |
| D-22 | O envio do formulário é a rota `POST /inscricoes` em `routes/web.php` | O projeto não tem `routes/api.php`; criar um exigiria instalar o pacote de API só para uma rota. A proteção contra envio forjado (CSRF) do grupo `web` é bem-vinda em um formulário público |
| D-23 | `ExpirarInscricoesVencidas` foi antecipada da etapa de pagamento para a etapa de inscrição | A varredura sob demanda (quando o contador diz "lotado") depende dela. A versão atual apenas muda a situação e devolve as vagas; cancelar a cobrança e anunciar a expiração entram junto com o domínio de pagamento |
| D-24 | Envio repetido com a mesma chave de idempotência responde **200**, e não 201 | Nada de novo foi criado. O participante recebe a mesma inscrição, e o código de resposta diz a verdade sobre o que aconteceu |
| D-25 | O prazo de pagamento (`prazo_pagamento`) já é gravado na criação da inscrição | RN-P01 manda congelar o prazo no momento da inscrição. Sem ele gravado, a varredura sob demanda não teria como saber quais reservas venceram |
| D-26 | O provedor simulado guarda o estado de cada cobrança em **arquivo no disco local** (`storage/app/private/pagamentos-simulados`), não em memória nem em cache | A cobrança é criada em uma requisição e paga em outra, inclusive em processos diferentes (o teste de concorrência dispara processos de sistema operacional). Arquivo funciona em qualquer máquina, sem depender de Redis para desenvolver |
| D-27 | A cobrança é emitida **fora** da transação que cria a inscrição | Conversa com serviço externo não pode segurar uma transação de banco aberta. Se a emissão falhar, a inscrição já existe e a cobrança sai na tentativa seguinte, porque `CriarPagamentoDaInscricao` é repetível |
| D-28 | A rota do webhook fica **fora do grupo `web`** | Quem chama é um servidor, não um navegador: sem sessão e sem cookie, não há proteção de CSRF a satisfazer nem por que criar sessão |
| D-29 | As rotas de simulação têm **duas travas**: só são registradas em `local`/`testing` com a chave ligada, e ainda passam por um middleware que confere as duas condições de novo | Uma configuração trocada por engano em produção não pode abrir uma porta de "pagar sem pagar". A resposta é 404, não 403: quem procura a porta nem descobre que ela existe |
| D-30 | A chave estrangeira de `pagamentos` para `inscricoes` é **restrict** | Apagar inscrição já é proibido; a restrição do banco garante que nenhum histórico de dinheiro suma junto com um apagamento acidental |
| D-31 | O aviso do provedor é guardado com os campos sensíveis substituídos por `[removido]` (segredo, token, cartão, CVV) | O aviso serve para investigar divergência, não para colecionar dado sensível |
| D-32 | Os anúncios internos (`InscricaoConfirmada`, `InscricaoExpirada`) são disparados **depois** que a transação fecha, e só na chamada que de fato mudou a situação | Ninguém pode ser avisado de um fato que o banco ainda pode desfazer. E, como a mudança é condicional à situação anterior, aviso repetido do provedor não gera segundo anúncio |
| D-33 | A reconciliação roda **a cada cinco minutos** e olha apenas cobranças a até quinze minutos do vencimento | Perguntar de cinco em cinco minutos reconhece o pagamento bem antes do prazo vencer, e a margem estreita mantém o volume de consultas baixo — provedores cobram limite de chamadas por minuto |
| D-34 | A reconciliação **fecha** aqui a cobrança que o provedor já deu por vencida, mas **não devolve vaga** por conta própria | Vaga é assunto da inscrição, e quem devolve é sempre a expiração. Duas rotinas mexendo no mesmo contador seria a receita para contar errado |
| D-35 | A expiração processa em lotes de 100 com `chunkById` e cada inscrição em sua própria transação | Uma transação única sobre milhares de linhas seguraria bloqueios por tempo demais. Falha em uma inscrição não derruba o lote inteiro |
| D-36 | O site público não guarda rascunho: as quatro etapas são navegação na própria tela e só o envio final grava | O backend cria a inscrição inteira em um `POST` só, dentro de uma transação. Guardar meio caminho exigiria uma segunda máquina de estados e a chance de duas verdades sobre a mesma pessoa |
| D-37 | A `chave_idempotencia` nasce no navegador (`crypto.randomUUID()`) quando o formulário abre e acompanha todas as tentativas | Duplo clique, conexão que cai e reenvio passam a devolver a mesma inscrição, e não uma segunda |
| D-38 | Quem escolhe qual das três telas de cobrança aparece (aguardando, confirmada, expirada) é o **servidor**, por uma propriedade `estado` calculada a partir do que o domínio gravou | Nenhum parâmetro vindo do navegador pode declarar que algo foi pago. A tela só desenha o que o servidor decidiu |
| D-39 | As vagas mostradas na tela são um retrato do momento em que a página carregou; não há consulta contínua de vagas | A autoridade é a revalidação no envio, que já existe e já devolve 422 em linguagem simples. Consulta contínua custaria banda no celular por uma informação que muda pouco |
| D-40 | Toda cor vive em token semântico (`--cor-acao`, `--cor-sucesso`, `--cor-informacao`, `--cor-atencao`), definido uma vez para o modo claro e redefinido para o escuro; nenhum componente escreve cor literal | Trocar a paleta passa a ser editar um arquivo. E é o único jeito de garantir o contraste nos dois modos sem caçar cor espalhada por dezenas de telas |
| D-41 | O vermelho de erro do pacote inicial (`#EF4444`) foi escurecido para `#D31212` no modo claro | O tom original rende 3.76:1 sobre branco e reprova em texto pequeno — justamente onde o participante lê "Este CPF não parece válido". O novo passa a 5.43:1. No modo escuro o tom original já cumpre (5.24:1) e ficou como estava |
| D-42 | A suíte de ponta a ponta usa o banco `testing`, o mesmo do Pest, e o recria antes de começar | Criar um banco próprio exigiria um passo de instalação a mais em cada máquina. O preço é não rodar `npm run test:e2e` e `php artisan test` ao mesmo tempo — está escrito no cabeçalho de `tests/e2e/ambiente.ts` |
| D-43 | O `tsconfig.json` do pacote inicial listava `"vue/tsx"` e `"./resources/js/types"` no campo `types`, que só aceita nome de pacote de tipos. Foram removidos na Fase 5a | Com as entradas inválidas, a verificação de tipos **abortava antes de checar qualquer arquivo**: o projeto atravessou as Fases 0 a 4 sem verificação nenhuma, exibindo apenas dois erros de configuração que pareciam ruído conhecido. Corrigido, o `vue-tsc` passou a rodar de verdade e revelou 20 erros reais, todos em telas do pacote inicial (ver **P-09**) |
| D-44 | A Fase 5b antecipou **apenas o e-mail do link de acesso**. Os demais e-mails — confirmação, comprovante, prazo vencido — e os ouvintes dos três anúncios de domínio continuam na Fase 7 | Sem esse e-mail, quem fecha o navegador perde a inscrição de vista, e a área do participante nasceria sem porta de entrada. Os outros e-mails não têm essa urgência e pedem fila, modelo visual e lembretes agendados — trabalho que a Fase 7 faz de uma vez. A decisão D-12 continua de pé: os anúncios `InscricaoCriada`, `InscricaoConfirmada` e `InscricaoExpirada` seguem sem nenhum ouvinte |
| D-45 | **Cancelamento da inscrição pela própria participante ficou fora da Fase 5b**, e continua como pendência do dono do produto | Cancelar mexe em vaga, em dinheiro já pago e em política de reembolso (pendências P-02 e P-04) — não é tela, é regra de negócio. Consequência prática: a fase inteira não criou **nenhuma Action, Enum ou migração**, e a única escrita que a área do participante faz é pedir a segunda via do Pix |
| D-46 | O acompanhamento ganhou **rota própria** (`/inscricoes/{código}/acompanhar`), em vez de crescer dentro da tela de pagamento | A tela de pagamento tem um trabalho só: mostrar o Pix e avisar quando ele cai. Misturar histórico e linha do tempo ali obrigaria a reescrevê-la e a refazer os 12 cenários Playwright da Fase 5a. Com rota separada, os 12 ficaram **intactos** e as duas telas apenas se ligam por link |
| D-47 | A **segunda via do Pix reemite sob demanda** chamando `CriarPagamentoDaInscricao`, que já é repetível: havendo cobrança pendente, devolve a mesma; não havendo, emite outra com o mesmo prazo | Fecha o buraco deixado pela decisão D-27. Como a cobrança é emitida fora da transação que cria a inscrição, uma falha de conexão pode deixar a inscrição de pé e sem cobrança nenhuma. Antes, essa pessoa não tinha saída; agora ela mesma resolve com um toque, sem ganhar prazo novo e sem mexer em vaga |
| D-48 | A resposta neutra do pedido de link de acesso é garantida por **três medidas juntas**: o limite de tentativas é contado dentro do controller (nunca por um 429 do middleware), o e-mail vira impressão digital antes de virar chave do limite, e há um piso de tempo de resposta de 400 ms; falha no envio é registrada no log e engolida | Cada brecha sozinha entrega a mesma informação: um 429 confirma que o endereço acertou o alvo, uma resposta rápida demais denuncia que nenhum e-mail foi enviado, e um erro 500 conta que a inscrição existe. Sem as três, o formulário viraria uma máquina de descobrir quem está inscrito |
| D-49 | Nas telas do participante, os títulos de cartão são `<h2>` escritos à mão, e não o componente `CardTitle` do pacote inicial | O `CardTitle` desenha um `<h3>`. Depois do `<h1>` da página, isso pularia um nível, e quem navega por leitor de tela usa exatamente esses níveis como sumário. O texto de recuperação de acesso ganhou o mesmo cuidado: o campo aponta ao mesmo tempo para a ajuda e para o erro (`aria-describedby="ajuda-email erro-email"`), em vez de trocar uma coisa pela outra e fazer a dica sumir justo quando ela é mais necessária |
| D-50 | Autorização com **`spatie/laravel-permission`**, com dois papéis (`administrador`, `organizador`) e nove permissões. As tabelas do pacote ficam em inglês (`roles`, `permissions`, `model_has_roles`); os **nomes dos papéis e das permissões são em português** | É a primeira dependência pesada do projeto, e o custo foi assumido de olhos abertos: escrever à mão papéis, permissões, cache e middleware seria reimplementar um pacote maduro para chegar ao mesmo lugar. As tabelas em inglês são coerentes com a **D-02** — estrutura de ferramenta fica em inglês; o que é do negócio fica em português, e "quem pode confirmar pagamento" é do negócio. Dois papéis, e não cinco: tesoureiro, credenciador e "só leitura" são fáceis de acrescentar depois, porque as permissões já são granulares; criar perfil sem alguém para ocupá-lo é inventar requisito |
| D-51 | **Cadastro público fechado.** As rotas de registro, a tela `Register.vue` e o `RegisteredUserController` foram removidos; `tests/Feature/Auth/RegistrationTest.php` **passa a provar o 404** nas duas rotas, em vez de ser apagado; a conta administrativa nasce pelo comando `usuario:criar-administrador` | A tela de cadastro veio no pacote inicial e ninguém a pediu: enquanto existisse, qualquer visitante criaria uma conta do lado de dentro. Manter o arquivo de teste, com o propósito invertido, é o que impede que alguém reponha a rota sem perceber — a porta trancada passa a ter alarme. A senha é pedida de forma escondida porque senha em argumento de comando fica gravada no histórico do terminal |
| D-52 | Os **20 erros de tipo da pendência P-09** foram corrigidos nesta fase, e não adiados de novo | Eles viviam exatamente nas telas do pacote inicial que a Fase 6a passa a usar de verdade — barra lateral, cabeçalho, menu da conta, telas de autenticação. Construir o painel sobre código que a verificação de tipos nunca leu seria empilhar em cima de chão não conferido. Corrigir foi **tipar de verdade**: nenhum erro foi silenciado com `any`, `as unknown as` ou `@ts-ignore` |
| D-53 | Os tipos compartilhados do pacote inicial foram **corrigidos, não contornados**: `Auth.user` passou a ser `User \| null`, `SharedData` ganhou índice aberto e o `NavItem` duplicado dentro de `NavMain` foi eliminado | Ao tipar, apareceu um defeito de verdade: `NavMain` tinha uma cópia própria de `NavItem` com o campo chamado `url`, enquanto o resto do projeto usa `href`. O item da barra lateral **nunca acendia como ativo** — não por decisão de design, mas porque os dois tipos nunca conversaram. `Auth.user` também mentia: a página de boas-vindas é aberta por visitante, e ali não há usuário nenhum. Onde o valor pode faltar, quem consome trata a falta; onde a tela só existe atrás do login, o valor de partida é vazio em vez de forçado |
| D-54 | A declaração das variáveis de ambiente do Vite (`ImportMetaEnv`) saiu de dentro de `app.ts` e virou o arquivo `resources/js/types/vite-env.d.ts` | O pacote inicial tentava aumentar `vite/client` com `declare module`, e isso simplesmente não funciona: `vite/client` é um arquivo de declarações globais. Declarada no escopo global, a interface se junta à definição do próprio Vite — que é o comportamento que o código original esperava e nunca teve |
| D-55 | A decisão em aberto **DA-13 foi promovida a decisão tomada**: o lado de dentro ganhou **exatamente duas** ações sobre a inscrição de outra pessoa — **cancelar** e **confirmar pagamento na mão** —, as duas com justificativa escrita obrigatória, e a segunda **exclusiva do `administrador`** (permissão `pagamentos.confirmar-manual`) | Duas ações, e não um menu de operações, porque cada coisa que o lado de dentro faz sobre a inscrição alheia é uma coisa que pode ser feita por engano ou de má-fé. O motivo é obrigatório porque ação administrativa sem justificativa é rastro que não explica nada — e é exatamente esse texto que a Fase 9 vai transformar em registro de auditoria. Confirmar pagamento manualmente é a única ação do sistema que declara "entrou dinheiro" sem fonte externa nenhuma; quanto menos gente puder fazer isso, melhor |
| D-56 | Cancelar uma inscrição **já confirmada** é permitido, e **não gera estorno nenhum**. A ficha avisa isso com todas as letras antes de a pessoa apertar o botão | O organizador precisa cancelar quem desistiu depois de pagar — recusar isso empurraria o problema para fora do sistema. Mas devolver dinheiro depende de uma política que **não existe** (pendência **P-02**), e software não toma decisão financeira por conta própria. O pagamento continua registrado como recebido, a vaga volta para a fila, e a devolução (se houver) é conversa entre pessoas. A pendência P-02 **segue aberta** |
| D-57 | A confirmação manual **recusa** inscrição já expirada, com mensagem em português que diz o que fazer no lugar ("faça uma nova inscrição") | Quando o prazo vence, a vaga já voltou para a fila e **pode ter dono novo**. Confirmar por cima seria vender a mesma vaga duas vezes — o erro mais caro que este sistema pode cometer. Esse é justamente o assunto da pendência **P-03**, que **segue aberta**; a recusa é a leitura segura enquanto ninguém decide, e é reversível: no dia em que houver política, basta trocar a recusa por ela |
| D-58 | Nasceu o anúncio de domínio **`InscricaoCancelada`** (com a inscrição, o motivo, o responsável e se ela estava confirmada) e, como os outros três, ele **não tem nenhum ouvinte** ainda. A confirmação manual **não** criou anúncio próprio: reaproveita o `InscricaoConfirmada` de sempre | A decisão **D-12** continua de pé — quem escuta os anúncios é a Fase 7, e ela vai encontrar o de cancelamento já sendo disparado, sem precisar mexer em regra de domínio. Anúncio próprio para a confirmação manual seria criar dois nomes para o mesmo fato: para o resto do sistema, a pessoa está confirmada, e só isso importa. **De onde veio o dinheiro fica registrado no pagamento**, que é onde essa informação faz diferença |
| D-59 | O `MetodoPagamento` ganhou três formas novas — `dinheiro`, `transferencia` e `outro` — e **Pix e cartão não podem ser declarados na mão** | Sem isso, um pagamento em espécie teria de ser gravado mentindo que foi Pix. As três formas novas existem só para o dinheiro que entra **por fora** do sistema; quando a cobrança passa pelo provedor, quem reconhece o dinheiro é o provedor, e não uma pessoa clicando num botão. O plano da fase não previa mexer no Enum — a necessidade apareceu na primeira linha de código da confirmação manual |
| D-60 | Quando **não há cobrança aberta** (o Pix venceu e ninguém pediu segunda via), a confirmação manual **abre uma cobrança própria**, com `gateway = 'manual'` e `id_externo` vazio, em vez de recusar | O dinheiro entrou de verdade e precisa de um lugar para ser registrado; recusar deixaria uma pessoa paga e não confirmada, sem saída. O registro diz com todas as letras que não houve provedor nenhum, e é por isso que o identificador externo fica vazio: inventar um número de provedor seria falsificar histórico de dinheiro |
| D-61 | O cancelamento faz **duas gravações condicionais**, uma exigindo `aguardando_pagamento` e outra exigindo `confirmada`, em vez de uma só que aceitasse as duas situações | Porque a vaga devolvida é **diferente** em cada caso: quem estava aguardando prendia vaga reservada, quem estava confirmada ocupava vaga paga. Uma condição única não saberia qual contador mexer, e a única forma de descobrir seria ler a linha antes de escrever — que é exatamente a brecha por onde entra a devolução em dobro quando dois processos agem no mesmo instante |
| D-62 | Os marcadores usados pelos testes de tela são `data-testid`, **em inglês**, e não `data-teste` | Exceção consciente à **D-01**: `data-testid` não é vocabulário do negócio, é o nome que a ferramenta de teste lê. Escrito em português, o seletor simplesmente não encontrava nada, e os três cenários novos falhavam por tempo esgotado — sem dizer o motivo, porque "não achei o elemento" é a mesma mensagem de "o elemento não apareceu" |
| D-63 | A coluna de conteúdo do layout administrativo recebeu `min-w-0`, e o link "Abrir" da tabela de inscrições ganhou âncora de posicionamento própria (`relative`) | Descoberto ao consertar os cenários novos, que rodam num navegador do tamanho de um celular. Sem `min-w-0`, a coluna se recusava a ficar mais estreita que a tabela mais larga dentro dela, e o navegador reagia afastando a página inteira — os campos ficavam fora de alcance e o teste esgotava o tempo esperando poder digitar. O segundo é irmão do primeiro: o texto que só o leitor de tela ouve é posicionado de forma absoluta e, sem âncora por perto, se prendia à página inteira e a esticava. **Nas duas vezes o teste não estava exigindo demais: a tela estava mesmo quebrada no celular** |
| D-64 | O diálogo de ação **devolve o foco ao botão que o abriu por conta própria**, guardando quem estava em foco na hora da abertura | O componente de diálogo do projeto devolve o foco sozinho quando é aberto por um gatilho próprio — mas o nosso é comandado por uma propriedade, e nesse formato ninguém sabe de onde a pessoa veio. Sem isso, quem navega por teclado fechava o diálogo e caía no começo da página, perdendo o lugar onde estava. O cenário que aperta `Esc` e confere onde o foco parou é o que impede a regressão |
| D-65 | A decisão em aberto **DA-14 foi promovida a decisão tomada**: a Fase 7 entrega **exatamente cinco** e-mails ao participante — inscrição recebida, lembrete de prazo, pagamento confirmado, prazo vencido e inscrição cancelada. Nenhum aviso ao organizador, nenhum resumo periódico, nenhum descadastro | Cinco porque são os cinco momentos em que a pessoa fica sem saber o que aconteceu com o dinheiro ou com a vaga dela. Aviso ao organizador é outro público, com outra frequência e outro conteúdo — vira ruído se for tratado como caso particular deste. E **não há opção de descadastro** porque nenhuma das cinco é divulgação: todas são consequência direta de um ato da própria pessoa ou de uma decisão da organização sobre a inscrição dela. Marketing exigiria descadastro; isto não é marketing |
| D-66 | A trava contra e-mail repetido é a **unicidade `(inscricao_id, tipo, canal)` no banco**, e o registro é gravado **antes** do envio, na mesma transação — nunca uma verificação "já mandei?" escrita em PHP | Dois trabalhadores da fila podem pegar o mesmo trabalho no mesmo instante: os dois passariam pelo mesmo `if` e a pessoa receberia duas cópias. Só o banco consegue arbitrar. Gravar antes de enviar inverte o intuitivo de propósito — se o banco recusar, alguém chegou primeiro e o trabalho encerra em silêncio; se o envio falhar, a transação volta atrás e o registro some junto, deixando o caminho livre para a próxima tentativa. O registro só sobrevive se a mensagem de fato entrou no caminho de entrega |
| D-67 | Todo e-mail sai pela **fila `emails`**, separada da fila padrão, com 3 tentativas e espera de 1, 5 e 15 minutos | Fila porque servidor de e-mail lento não pode atrasar uma inscrição — o e-mail é consequência do fato, não parte dele. Fila **própria** para que um dia um trabalhador dedicado a e-mail não dispute vez com outro tipo de trabalho. As três tentativas cobrem o caso comum (servidor fora do ar por alguns minutos) sem inundar a fila. **Contrapartida assumida:** enquanto ninguém subir um trabalhador, nenhum e-mail sai — está escrito no `.env.example`, na seção 9.1 do `ARCHITECTURE.md` e no topo deste arquivo |
| D-68 | Nenhum corpo de e-mail carrega CPF, impressão digital do CPF, telefone ou o código Pix inteiro; e o e-mail de cancelamento **não recebe nem sequer** o texto do motivo interno | E-mail é encaminhado, impresso e guardado por anos: o que ele leva é link assinado, que vence sozinho e pode ser trocado (mesma regra da D-44). O motivo do cancelamento é anotação administrativa e pode conter observação que não se escreve para a pessoa — a proteção mais forte não é lembrar de não imprimir o texto, é o Mailable **não ter acesso a ele**. Um teste renderiza os cinco corpos, em HTML e em texto puro, e procura por cada um desses dados |
| D-69 | **`LinkDeAcessoInscricao` continua síncrono**, mesmo agora que os outros cinco e-mails vão para a fila | Três motivos, e o terceiro decide. Primeiro: ele responde a um pedido humano imediato — a pessoa está parada na tela esperando o link. Segundo: o custo já está limitado pelo piso de tempo de resposta de 400 ms (D-48), então enfileirar não economizaria tempo nenhum. Terceiro: enquanto nenhum trabalhador estiver de pé, enfileirar este e-mail seria o mesmo que deixar de enviá-lo — e ele é o único e-mail que funciona hoje. Migrá-lo também obrigaria a reescrever os testes de resposta neutra do `POST /acesso`, que são a guarda de uma propriedade de segurança: reescrever guarda de segurança em troca de uniformidade é mau negócio |
| D-70 | O gancho de WhatsApp é **só a coluna `canal` e o Enum `TipoComunicacao`**. Nenhum contrato, adaptador ou configuração de canal foi criado | Contrato de canal com uma implementação só é abstração inventada: ela envelhece antes de ser usada e, quando o segundo canal aparecer de verdade, quase nunca cabe no formato imaginado antes. O que esta fase entrega é o que **não** precisará ser desfeito: uma tabela que já sabe distinguir por onde a mensagem saiu e um vocabulário de tipos de mensagem |
| D-71 | Fila, conexão, tentativas e espera dos ouvintes são **métodos** (`viaQueue()`, `viaConnection()`, `tries()`, `backoff()`), e não propriedades definidas no construtor | Não é estilo, é defeito consertado. Para decidir em qual fila colocar um ouvinte, o Laravel cria uma cópia dele **sem chamar o construtor**; com os valores vindo do construtor, a fila lida era nula e o trabalho caía na `default` — que um trabalhador dedicado à fila `emails` nunca veria. O teste que tranca isso monta a mesma cópia sem construtor que o Laravel monta, em vez de perguntar a um objeto bem-comportado |
| D-72 | O **lembrete é único por inscrição**, e essa regra está anotada no Enum `TipoComunicacao` como aviso, não implementada por antecipação | Enquanto o prazo de pagamento não puder ser esticado — e hoje não pode —, um lembrete por inscrição é o comportamento certo. No dia em que existir prorrogação, um prazo novo pede um lembrete novo e a chave de unicidade precisará mudar junto. Escrever hoje o código desse dia seria manter uma regra que ninguém usa e ninguém testa |
| D-73 | A **descoberta automática de ouvintes ficou desligada** (`withEvents(discover: false)`); a ligação anúncio → ouvinte é escrita à mão no `AppServiceProvider` | Descoberta automática esconde justamente a ligação mais importante do sistema: qual fato do domínio faz sair uma mensagem para uma pessoa de verdade. Escrita à mão, ela se lê em cinco linhas — e uma classe nova em `app/Listeners` não começa a receber anúncios sem alguém decidir que ela deve |
| D-74 | O e-mail de **prazo vencido** aponta para a **página do evento**, e não para a inscrição encerrada | A única ação útil para quem perdeu o prazo é se inscrever de novo, se ainda houver vaga. Mandar de volta para uma inscrição que não aceita mais pagamento seria oferecer um caminho sem saída |
| D-75 | A chave estrangeira de `comunicacoes_enviadas` para `inscricoes` é **`restrict`**, como a de `pagamentos` | Pela mesma razão: o registro de que uma mensagem foi enviada é prova de comunicação com uma pessoa. Se um apagamento acidental pudesse levá-lo junto, a proteção contra mensagem repetida sumiria com ele |
| D-76 | A decisão em aberto **DA-15 foi promovida a decisão tomada**: **a LGPD ficou fora da Fase 9**. Retenção, prazo de descarte e anonimização **não foram implementados**, e isso está dito em letra grande no topo deste arquivo | Descarte de dado pessoal é decisão jurídica, não técnica. Implementar sob um prazo inventado seria o software decidindo por quanto tempo a organização guarda o CPF de um menor de idade — e, uma vez apagado, não há volta. Depende da **P-04** (prazo de retenção e descarte) e da **P-03** (pagamento reconhecido depois do prazo), as duas do dono do produto. Vira plano próprio quando forem respondidas. Registrar a ausência explicitamente é parte da decisão: fase de "endurecimento" concluída faz qualquer pessoa supor que privacidade foi tratada |
| D-77 | O model `LogAuditoria` **recusa `update` e `delete`**, sempre, lançando exceção nos eventos do Eloquent. Sem flag, sem modo administrador, sem exceção "só desta vez" | Registro que pode ser corrigido depois não prova nada: bastaria alguém apagar a própria linha para a auditoria virar enfeite. A trava está no model, e não num combinado com a equipe, porque combinado não sobrevive a quem escrever código novo sem ler esta decisão. Quem tiver acesso direto ao banco continua podendo mexer na tabela, e isso é proposital — a defesa nesse nível é permissão de banco, não código PHP |
| D-78 | **Auditoria é testemunha, não porteiro:** falha ao gravar o log **não** desfaz nem impede a ação administrativa (o erro vai para o log da aplicação e a ação segue). E o campo `dados` guarda **o nome do campo que mudou, nunca o conteúdo sensível** — sem CPF, impressão digital, senha, token ou Pix inteiro | Se a auditoria pudesse derrubar a ação, ela viraria ponto único de falha do painel: uma tabela cheia ou uma restrição violada impediriam cancelar inscrição e confirmar pagamento. O prejuízo de um log perdido é uma linha de histórico; o de um painel travado é operação parada. Quanto ao conteúdo: um log que ninguém pode apagar e que guarda CPF é uma pilha permanente de dado pessoal — a trava da D-77 trabalharia contra a privacidade em vez de a favor da prestação de contas |
| D-79 | Depois de medir com 10.000 inscrições no banco, **nenhum índice novo foi criado e nenhum cache foi ligado** | Não é "não mexemos", é "medimos e concluímos que mexer pioraria". Todo índice custa na escrita, e escrita é o caminho da inscrição — o único que não pode ficar lento, porque é o que a pessoa faz enquanto disputa uma vaga. Índice sem medição que o justifique é custo garantido em troca de benefício imaginário. E o painel responde em 5 ms: um cache de 60 segundos economizaria 5 ms em troca de mostrar número velho ao organizador no dia em que ele mais precisa do número certo. Como não há cache, também **não há aviso de atraso na tela** — e não deve haver, porque seria mentira. Os números de cada consulta estão em `docs/PERFORMANCE.md` |
| D-80 | `POST /inscricoes` tem **dois limites por endereço de internet ao mesmo tempo**: um por minuto, folgado, e um por hora, apertado. A recusa é uma frase **em português**, entregue como erro do formulário para o Inertia e como JSON para os demais | Um limite só obrigaria a escolher entre punir a família que sai pela mesma conexão e deixar passar o programa automatizado. Dois resolvem: o do minuto absorve o pico humano legítimo; o da hora é o que segura o script, porque nenhuma família manda sessenta inscrições numa hora e nenhum programa disfarça isso esperando entre os pedidos. A frase em português existe porque a página crua de "429 Too Many Requests" não diz a quem está tentando se inscrever o que fazer em seguida |
| D-81 | O login administrativo ganhou teto por **endereço de internet**, por cima do limite por e-mail que o Laravel já trazia; e o webhook de pagamento ganhou teto alto por endereço, **sem alterar a D-18** | O limite do Laravel conta tentativas por e-mail: quem varre uma lista de e-mails diferentes do mesmo lugar nunca esbarraria nele, que é exatamente o ataque mais comum. Os dois somados cobrem os dois formatos. No webhook, o teto é alto porque quem chama é um servidor e várias confirmações juntas são o dia movimentado, não um ataque — e a recusa por excesso acontece **antes** de o aviso ser lido, então ela continua sem contar nada sobre a assinatura: aviso com assinatura inválida segue recebendo 200 |
| D-82 | O middleware `CabecalhosDeSeguranca` é registrado **globalmente**, não no grupo `web`. A CSP só é enviada em resposta **HTML**, e o `Strict-Transport-Security` só em resposta **HTTPS** | A rota do webhook fica fora do grupo `web` de propósito (D-28): cabeçalho de segurança que depende de a rota estar no grupo certo é cabeçalho que um dia vai faltar justamente na rota esquecida. Já a CSP num CSV baixado ou num JSON de webhook não protege nada e só aumenta a resposta. E mandar HSTS em desenvolvimento (http) faria o navegador guardar por um ano que o endereço é sempre seguro — a pessoa passaria dias sem conseguir abrir o próprio ambiente local |
| D-83 | A CSP libera script por **número de uso único sorteado a cada resposta** e **não tem `unsafe-inline` em `script-src`**. A tabela de rotas do Ziggy passou a receber esse número (`@routes(nonce: ...)`). O `style-src` **tem** `'unsafe-inline'`, como concessão declarada. Em `local`, com o servidor do Vite no ar, o endereço dele entra na política | É o tipo de mudança que funciona na máquina de quem desenvolve e quebra depois do deploy, então ela foi calibrada com Playwright, no navegador, e não no olho: um cenário abre a tela de pagamento com a CSP ligada e confere o QR Code do Pix, e outro confere a recuperação de acesso. Duas coisas quebraram e foram consertadas **entendendo o problema, não afrouxando a regra** — o Ziggy escreve script na própria página e precisava do número; e componente Vue escreve `style="..."` direto no elemento (barra de progresso, altura calculada, cor de estado), o que sem a concessão quebraria as telas visualmente em produção. CSS injetado permite disfarce visual, o que é ruim; JavaScript injetado permite roubar sessão e enviar formulário no lugar da pessoa, o que é muito pior — a defesa fica onde o estrago é maior. A exceção do Vite existe **só** enquanto o servidor de desenvolvimento estiver no ar e some sozinha em qualquer outro caso: sem ela, a tela ficaria em branco para quem desenvolve, e a reação natural seria afrouxar a CSP inteira |
| D-84 | A máquina de disputa por processos de verdade foi **extraída para `tests/Feature/Inscricoes/Disputa.php`** e é compartilhada pelo `ConcorrenciaTest` (6 processos) e pelo novo `CargaTest` (50 processos) | Os dois testes precisam exatamente da mesma coisa difícil de montar: um cenário gravado por uma conexão que os outros processos enxergam, os processos largando juntos e a limpeza depois. Copiar isso para o segundo teste criaria duas versões da mesma armadilha — e no dia em que uma fosse corrigida, a outra continuaria errada em silêncio. Extrair também deixou claro qual é a diferença real entre os dois: só o número de processos e o número de vagas. O `ConcorrenciaTest` foi refatorado para usar a peça compartilhada e continua provando, verde, exatamente o que provava antes |
| D-85 | O `esbuild` subiu de **0.24.2 para 0.25.12** por `npm audit fix`, sem troca de versão maior de nenhum framework | Era o único aviso de segurança das duas auditorias (`composer audit` não apontou nada). É ferramenta de construção — não vai para o navegador de ninguém —, e a correção coube dentro do que já estava permitido no `package.json`, então mexeu só no `package-lock.json`. O empacotamento das telas foi rodado depois e continua funcionando; ao final, `npm audit` reporta zero vulnerabilidades |
| DA-16 | O provedor de pagamento real é a **Efí**, API Pix. Fecha a pendência **P-01**, aberta desde a Etapa 1 | Decisão do dono do produto, em 2026-08-27. A matriz de comparação de `docs/PAYMENTS.md` continua no documento como registro do porquê, e como ponto de partida caso um dia seja preciso trocar. O que a escolha destravou não foi só a integração: era a última fase do plano que dependia de decisão de negócio |
| DA-17 | `parseWebhook()` passou a devolver uma **lista** de eventos, e não um evento só. O controller desdobra o aviso em um registro e um trabalho por pagamento | O aviso da Efí é uma lista, e um único POST pode trazer vários pagamentos. Devolver apenas o primeiro perderia dinheiro **em silêncio** — o pior jeito de perder, porque ninguém reclama: a pessoa pagou, o dinheiro entrou na conta e a vaga nunca foi confirmada. Foi a única mudança de contrato da fase, e ela obrigou a tocar no trabalho em segundo plano: **uma linha**, porque o payload que chega até ele passou a ter exatamente um item. Nenhuma regra mudou |
| DA-18 | **Estorno fora desta fase.** `refundPayment()` lança "não suportado", em voz alta. Mas o `endToEndId` passa a ser **guardado desde já** | Devolução depende da política de reembolso (**P-02**), que não foi decidida — implementar antes disso é construir um botão que ninguém sabe quando apertar, e ainda tomar decisão de negócio no código. Já o `endToEndId` é diferente: ele **só chega no aviso** de pagamento e some depois. Não guardá-lo agora significaria que, no dia em que a P-02 for decidida, **nenhum pagamento anterior teria como ser devolvido**. Custa um campo num `jsonb` que já existia; não custar nada agora e custar tudo depois é o tipo de conta que só se enxerga escrevendo |
| DA-19 | **Não existe ambiente publicado.** O código sai completo, mas nada foi ligado contra dinheiro de verdade. O que a Efí exige do servidor virou roteiro em `docs/ARCHITECTURE.md`; registrar o endereço do aviso no painel da Efí é **tarefa de implantação** | Mesmo raciocínio do trabalhador da fila, na Fase 7: a parte que não é código precisa estar escrita em algum lugar, ou vira a descoberta de alguém às três da manhã. O detalhe que mais custaria caro se ficasse implícito: sem o endereço do aviso registrado, **as cobranças nascem normalmente e nenhuma se confirma** — e nada no sistema reclama. O sinal seria a reconciliação confirmando tudo cinco minutos depois, que é a rede de segurança e não o caminho normal |
| DA-20 | A suíte automatizada usa um **duplo** do cliente da Efí — sem credencial, sem certificado, sem rede. Para provar contra a Efí de verdade existe `php artisan efi:diagnostico`, travado fora de `local` e `testing` | Suíte que depende de credencial é suíte que não roda: quebra na máquina de quem acabou de clonar o projeto e no processo de integração, e a reação natural é desligá-la. Mas duplo prova desenho, não prova que a credencial funciona — daí o comando à mão, que confere certificado, token, cobrança e código Pix um passo por vez e diz qual falhou. Ele fica travado fora de desenvolvimento porque em produção criaria cobrança de verdade |
| DA-21 | Usar o **SDK oficial** `efipay/sdk-php-apis-efi`, e não o cliente HTTP do Laravel | Escolha do dono do produto. Ela tem uma consequência obrigatória que não é evidente: o SDK usa cliente HTTP próprio, e as ferramentas de teste do Laravel **não o interceptam**. Sem tratar isso, a suíte da fase inteira seria impossível sem credencial. Por isso o SDK ficou atrás de um embrulho fino, que é o único ponto do sistema que o instancia — e é ele que a suíte troca por um duplo. De quebra, se o SDK sair um dia, muda um arquivo |
| DA-22 | O certificado da aplicação é um **caminho de arquivo em variável de ambiente**, nesta fase. O arquivo fica fora do repositório | É o recorte honesto para uma fase que não tem ambiente publicado. A Fase 8b troca a fonte para o banco, cifrada, **sem que o gateway perceba** — por causa da DA-24 |
| DA-23 | A taxa Pix de **1,19% por transação** fica registrada **só** em `docs/PAYMENTS.md`, com a data e o endereço da consulta. A **P-06** continua aberta e **não bloqueou** a fase | Taxa não entra em código nem em configuração, por regra escrita desde a Fase 4: taxa muda e é negociável por volume, e um número desses em código vira decisão de negócio congelada por descuido. Como nada no sistema lê taxa, confirmar a taxa com o comercial não bloqueia a integração — bloqueia a precificação do evento, que é outra conversa |
| DA-24 | **Toda leitura de configuração da Efí passa por um lugar só** (`ConfiguracaoEfi`): ambiente, credenciais, certificado, chave Pix e segredo do aviso. Nunca configuração espalhada pelo gateway | É a decisão mais consequente da fase, e ela não tem a ver com pagamento. É a mesma ideia da D-17 aplicada à configuração: uma fronteira, um lugar para trocá-la. Se o gateway, o cliente e o comando de diagnóstico lessem configuração por conta própria, a Fase 8b — trocar a fonte do arquivo de ambiente para o banco — teria de reescrever os três. Com o lugar único, ela muda **o corpo de uma classe**. E não é promessa: existe teste que percorre `app/` inteiro e **falha** se um segundo arquivo aparecer lendo esse bloco de configuração, porque combinado que depende de memória não sobrevive à primeira pessoa que escrever código novo sem ler o combinado |
| DA-25 | A credencial **e o conteúdo do certificado** moram no **banco, cifrados** — não em variável de ambiente e não em arquivo solto no disco. O certificado é escrito em disco, com permissão restrita, **só no instante em que o SDK precisa dele** | Decisão do dono do produto. Variável de ambiente e arquivo solto no servidor têm o mesmo defeito, e ele só aparece no pior dia: **não entram no backup do banco**. Um contêiner recriado, uma máquina trocada ou um redeploy levam a configuração junto, e a descoberta acontece quando alguém tenta se inscrever. No banco, cifrada, a configuração viaja com o backup e sobrevive a qualquer uma dessas coisas. A cifra é **a mesma que já protege o CPF** (**D-08**), de propósito: mecanismo novo, chave nova e rotina de rotação própria seriam três coisas a mais para errar, e a chave da aplicação já vive fora do banco — que é o ponto todo. O arquivo materializado é **cache descartável**: pode ser apagado a qualquer momento e o sistema o reescreve na chamada seguinte, o que também significa que ninguém precisa se lembrar de copiá-lo para lugar nenhum |
| DA-26 | **O cadastro da tela vence; o arquivo de ambiente do servidor continua valendo como reserva**, e só quando **não há nenhum ambiente ativo cadastrado**. A reserva **não completa** o que falta num cadastro pela metade | Derivada da **DA-20**. Sem a reserva, a suíte automatizada e a máquina de quem acabou de clonar o projeto precisariam de um banco semeado com segredo para rodar qualquer coisa — que é exatamente o problema que a DA-20 evitou na 8a. A segunda metade da decisão é a que evita prejuízo: se a reserva completasse o que falta no cadastro, um cadastro pela metade cobraria com **meia credencial da tela e meia do servidor**, provavelmente de ambientes diferentes, e o sintoma seria uma cobrança que nasce e nunca confirma — sem erro em lugar nenhum. Cadastro incompleto **falha dizendo o que falta** |
| DA-27 | **Dois registros independentes**, `homologacao` e `producao`, e um indicador de qual está ativo. Trocar de ambiente é ação de tela, auditada — e passar para produção exige **digitar uma palavra de confirmação**, cobrada no servidor | Decisão do dono do produto. Um registro só obrigaria a apagar a homologação para testar produção, e a refazer tudo à mão para testar de novo — o que, na prática, faria ninguém testar. Dois registros também tornam impossível o erro mais caro desta integração, misturar credencial de um ambiente com a do outro, porque nada passa de um bloco para o outro na tela. A confirmação **escrita** existe porque clicar em "sim" virou reflexo para quem usa computador, e digitar não; e ela é cobrada **no servidor** porque confirmação que vive só na tela é confirmação que some no dia em que alguém chamar o endereço direto. Quem garante que só existe um ativo, no fim, não é nada disso: é um **índice único parcial do PostgreSQL** (mesma lição da **D-66**) |
| DA-28 | **O mTLS de verdade no aviso da Efí não será exigido no Traefik.** A defesa do endereço do webhook é dupla e diferente: o **HMAC** que a aplicação já confere em todo aviso (Fase 8a) mais uma **lista de IP** (`34.193.116.226`) num middleware do Traefik, aplicada **só** ao router do webhook | Decisão do dono do produto, e é **desvio consciente do que a Efí recomenda** — está escrito como tal aqui, em `docs/ARCHITECTURE.md` §13.4 e em `docs/DEPLOY.md` §8.2, para que ninguém o descubra por acaso daqui a seis meses. O que se ganha: exigir certificado de cliente na borda significa configurar a cadeia da Efí no Traefik e mantê-la, num Traefik que **atende outros sistemas do mesmo servidor** — um erro ali não quebra só este projeto. O que se perde: um aviso forjado que viesse do IP da Efí ainda precisaria acertar o HMAC, que a aplicação confere em tempo constante e que não trafega em lugar nenhum além da URL registrada no painel. E há o argumento da §8.3.2 valendo aqui também: **a aplicação é quem sabe recusar com registro**; cortar a conexão na borda troca uma recusa registrada por uma porta batida sem rastro |
| DA-29 | E-mail de produção pela **Resend**, com `resend/resend-laravel`, por **API HTTPS** — não por SMTP | Única dependência nova aceita na etapa, e justificada pelo que ela desbloqueia: é o que faz os **cinco e-mails da Fase 7 chegarem a alguém**. SMTP foi descartado por um motivo prático: as portas 25 e 587 costumam vir **bloqueadas** em servidor de nuvem, e a falha seria **silenciosa** — o trabalho ficaria na fila, ninguém receberia nada e nada apareceria errado na tela. É exatamente o modo de falhar que este sistema mais tenta evitar. Em desenvolvimento nada muda: continua o Mailpit |
| DA-30 | **PostgreSQL e Redis dentro do próprio stack**, com volume nomeado, em rede interna e **sem publicar porta no host** | Decisão do dono do produto. Um banco gerenciado seria melhor e custa dinheiro que este projeto não tem; um banco no stack sem porta publicada não é alcançável da internet de forma nenhuma — o único caminho de fora para dentro passa pelo Traefik e para no `app`. O preço está escrito: **backup é responsabilidade de quem opera**, e não existe ainda |
| DA-31 | A imagem é publicada no **GHCR** (`ghcr.io/walberjefferson/gerenciador_eventos_ccc`), por **GitHub Actions, a cada push na `main`** | O código já vive no GitHub; usar o registro do mesmo lugar dispensa credencial de terceiro no CI e faz o `GITHUB_TOKEN` efêmero bastar. **Nenhum segredo do sistema entra no build** — domínio, senha do banco, chave da Resend e credencial da Efí entram só em tempo de execução, pelo Portainer. Tags de versão (`vX.Y.Z`) existem para conseguir voltar atrás; a `:latest` é a que o stack puxa |
| DA-32 | **`app`, `worker` e `scheduler` em containers separados**, todos da mesma imagem, distinguidos por `CONTAINER_ROLE` e pelo `command`. **O `scheduler` nunca pode ter duas réplicas** | Pedido explícito do dono do produto. A alternativa — um container só com supervisor dentro — parece mais simples e cobra depois: um processo que morre não reinicia sozinho sem alguém ter configurado o supervisor certo, e o log vira três coisas misturadas. Separados, **cada processo cai e reinicia sozinho sem derrubar os outros**, e `docker logs` de cada um é o log de uma coisa só. A restrição do `scheduler` não é preciosismo: **duas rodadas do mesmo agendamento são dois lembretes de prazo para a mesma pessoa** |
| DA-33 | Domínio `inscricoes.cccista.com.br`, atrás do **Traefik já existente** no servidor, na rede `proxy` com certresolver `le` — os mesmos do `controle_tempo_ccc`, sobrescrevíveis por variável | Decisão do dono do produto. Reaproveitar o Traefik que já roda evita um segundo terminador de TLS na mesma máquina e um segundo lugar de onde emitir certificado. As labels seguem a forma do projeto de referência, que **está em produção**: inventar label do Traefik é o tipo de coisa que só falha depois do deploy. Rede e resolvedor viraram variável para que o dia em que o servidor mudar não exija editar o compose |
| DA-34 | A raiz do domínio é uma **landing própria com o evento em destaque**, e não um redirecionamento para a vitrine | Decisão do dono do produto. Quem chega de um link no WhatsApp precisa entender **onde está** antes de clicar; um redirecionamento entrega a pessoa dentro de uma página de evento sem lhe dizer que sistema é aquele. E a landing continua servindo nos dois casos em que não há para onde redirecionar: **nenhum** evento aberto e **mais de um** |
| DA-35 | Sem evento aberto, a página dá o aviso — *"No momento não há inscrições abertas"* — **mais o próximo evento**, quando existe um publicado com abertura futura, dizendo **a partir de quando** | Decisão do dono do produto. Só o aviso deixa quem chegou cedo demais sem saber se volta amanhã ou daqui a três meses — e essa pessoa não volta. A data de abertura transforma uma porta fechada em um compromisso |
| DA-36 | Link discreto **"Já fiz minha inscrição"**, apontando para `/acesso` | Decisão do dono do produto. A recuperação de acesso existe desde a Fase 5b, mas **só chegava lá quem soubesse a URL de cor** ou ainda tivesse o e-mail. O link fica no rodapé do conteúdo, discreto de propósito: ele é para quem já sabe o que quer, não para quem está decidindo se se inscreve |
| DA-37 | **Sem identidade visual nova** (*superada pela DA-40 na Etapa 21, quando o dono do produto escolheu o tema do shadcn studio*). A página usa os tokens e componentes que o projeto já tem; **nada de logo, marca ou paleta inventados** | Decisão do dono do produto. Marca é assunto de quem conduz a comunidade, não de quem escreve a tela — e uma paleta inventada numa página só faz o site parecer dois sistemas diferentes. A home reaproveita o `PublicoLayout`, o botão, o alerta e a etiqueta da vitrine, e por isso parece da mesma família |
| DA-38 | Com mais de um evento aberto, destaca-se **o de data de início mais próxima**; os demais aparecem numa lista enxuta abaixo, cada um levando à sua vitrine | Escolher "o primeiro do banco" é escolher por acaso: sem `ORDER BY`, a ordem que o PostgreSQL devolve pode mudar depois de uma atualização de linha, e a home passaria a destacar outro evento sem que ninguém tivesse mudado nada. O teste grava os eventos **propositalmente fora de ordem** para provar que o critério é a data |
| DA-39 | **Tailwind 4.x**, e não continuar no 3.4 | Decisão do dono do produto, tomada com a ressalva de risco apresentada e reafirmada. Uma versão maior de framework de CSS renomeia utilitários, e utilitário renomeado não dá erro — some. O que torna a decisão defensável é a rede que o projeto já tinha: 40 cenários de navegador e 542 testes |
| DA-40 | **Adotar o tema do shadcn studio**, inclusive substituindo as cores da logo da CCC | Decisão do dono do produto, reafirmada depois de a consequência ser exposta: o tema é azul, e o botão "Fazer inscrição" deixa de ser vermelho. É mudança de identidade visual, não de código — e por isso é decisão de quem encomenda, não de quem escreve a tela |
| DA-41 | As quatro cores semânticas (`acao`, `sucesso`, `informacao`, `atencao`) são **derivadas do tema novo**, não apagadas | O tema do studio tem `primary`, `destructive`, `chart-*` e `sidebar-*`, mas **não tem "sucesso" nem "atenção"** — e o projeto usa as quatro em dezenas de telas para dizer coisas diferentes: disponível, prazo correndo, aviso neutro. Apagá-las trocaria significado por cor bonita |
| DA-42 | **Acessibilidade não é negociável.** Toda cor semântica tem razão de contraste calculada e escrita ao lado; tom que reprova em AA é escurecido até passar | A decisão foi adotar o tema, não abrir mão de acessibilidade. É a mesma regra que já valia desde a Fase 5a, quando o vermelho do pacote inicial (3,76:1) foi escurecido porque é em texto pequeno que o participante lê "Este CPF não parece válido". Três tons do studio reprovaram e foram ajustados |
| DA-43 | **Duas migrações, dois planos.** O `radix-vue` → `reka-ui` fica para depois | Decisão do dono do produto. Uma mexe em CSS, tokens e build; a outra mexe em imports e componentes. Feitas juntas, um cenário vermelho não diria qual das duas o quebrou — e a suíte deixaria de ser rede de proteção para virar enigma |
| DA-44 | Os 23 componentes de interface são **adaptados à mão**, e o gerador do shadcn **não é rodado** nesta etapa | Vários componentes carregam ajuste próprio do projeto — comentário em português explicando o porquê, correção de acessibilidade — e o gerador reescreve o arquivo inteiro. Ele apagaria justamente o que custou a existir. O objetivo da etapa era **destrancar a porta** (corrigir o `components.json`), não passar por ela |
| D-86 | As **dez ocorrências** de `classe-[--variavel]` viraram `classe-(--variavel)` nos quatro componentes que as tinham, e a gaveta do celular recebeu um `!` para cravar os 288px. Duas provas novas nasceram junto: um cenário Playwright em **1280×800** e uma varredura do CSS construído dentro do Pest | **O defeito não era de layout, era uma classe que virou CSS inválido sem ninguém reclamar.** No Tailwind 3, `w-[--sidebar-width]` queria dizer "largura igual a essa variável"; na versão 4 a forma passou a ser `w-(--sidebar-width)`, e a antiga vira valor literal. O compilador **não acusa nada** — ele gera `width:--sidebar-width`, o navegador descarta, e a barra lateral fica sem largura: o espaço reservado para ela some, o conteúdo escorre por baixo, e a barra, que é `position: fixed`, passa a flutuar por cima. O botão de recolher fica no canto esquerdo do cabeçalho, ou seja, **exatamente debaixo da barra** — ele sempre esteve montado e funcionando, só não dava para alcançar. Os três sintomas que o dono do produto relatou eram o mesmo defeito visto de três ângulos. **Por que 44 cenários de navegador não pegaram isto:** o `playwright.config.ts` declara um único projeto, `celular` (Pixel 5, 393px). Abaixo de 768px o `Sidebar` nem renderiza esse trecho — vira uma gaveta. O caminho defeituoso **só existe em tela grande**, e tela grande nunca tinha sido aberta; por isso a correção vem com o primeiro cenário de tela grande da suíte, declarando o próprio viewport em vez de virar um projeto novo (um projeto novo faria os 44 cenários de celular rodarem duas vezes para provar uma coisa só). **O diagnóstico que abriu este trabalho errou num ponto, e o erro fica registrado aqui em vez de repetido:** ele afirmava que o caminho do celular "não usa nenhuma das classes quebradas" — é falso, e a linha 43 do `Sidebar.vue`, que o próprio diagnóstico mandava corrigir, é justamente a da gaveta do celular. Foi essa contradição que fez a gaveta mudar de largura sem ninguém esperar, e foi o que levou a decisão de volta ao dono do produto, como a **DA-45** manda |
| D-86.1 | O `!` da gaveta (`w-(--sidebar-width)!`) é **remendo declarado, com prazo de validade escrito ao lado**, e não a solução | Corrigir a linha 43 devolveu o CSS válido mas **não** devolveu os 288px: deu 295px. A causa é um segundo defeito, latente desde a Etapa 21 — o `tailwind-merge` instalado é o **2.6.0**, feito para o Tailwind 3, e ele não reconhece `w-(--sidebar-width)` como classe de largura. Antes, reconhecia a forma antiga e por isso apagava o `w-3/4` que o `SheetContent` traz de fábrica; agora deixa as duas classes na lista e o `w-3/4` vence no CSS. O dono do produto escolheu cravar os 288px, e o motivo não foram os 7 pixels: sem isso, a constante `SIDEBAR_WIDTH_MOBILE` — declarada no próprio componente — deixaria de governar a gaveta e viraria enfeite, e código que declara uma constante que não governa nada é uma mentira em que a próxima pessoa tropeça. A saída de verdade é a **P-11**, e o comentário ao lado do `!` diz isso a quem chegar depois. **Hoje a gaveta é o único lugar do projeto com essa disputa**: as dez ocorrências foram conferidas uma a uma |
| D-87 | O passo "Tests" do `.github/workflows/tests.yml` passou a chamar **`./vendor/bin/pest`**, e não `./vendor/bin/phpunit` | Não é preferência de ferramenta: **o passo estava quebrado e nunca rodou um teste**. A suíte é escrita em Pest, e o binário do PHPUnit morre ao carregar o primeiro arquivo, com "Please run [./vendor/bin/pest] instead" — antes do primeiro teste existir. Está assim desde o commit de bootstrap (**19f8abd**, 2026-08-20), ou seja, **desde sempre**, e ninguém percebeu porque o passo falha de um jeito que parece erro de ambiente. Entrou neste commit, e não num plano próprio, porque sem ele a varredura de CSS recém-escrita não rodaria em lugar nenhum: ela mora no Pest justamente porque o CI constrói os assets antes de testar, e essa premissa era falsa enquanto o executor não carregasse |
| DA-45 | **Aparência congelada.** A migração troca comportamento de primitiva, não visual; se uma tela mudar de aparência, é defeito | Vinha logo depois da Etapa 21, que mudou toda a paleta. Duas etapas seguidas mexendo no que se vê, e ninguém mais sabe dizer qual delas causou o quê. Foi por essa regra que um defeito de largura herdado da Etapa 21 **ficou registrado em vez de corrigido**: consertá-lo mudaria uma tela, e quem decide isso é o dono do produto. **A regra funcionou como devia:** o defeito foi levado a ele, ele decidiu, e a correção virou a **D-86** — e no meio dela a mesma regra fez o trabalho parar outra vez, quando a gaveta do celular mudou de largura sem ninguém ter previsto (**D-86.1**) |
| DA-46 | A proposta de redesign das telas públicas (Claude Design, projeto "Redesign de evento e formulário") entra pelo **layout**, e a paleta continua sendo a do projeto | Decisão do dono do produto: "implementa sem substituir as cores". A proposta vem sobre o design system **e-SUAS Arapiraca**, que é azul-marinho `#163c84` com ciano — cores calculadas para outro produto. As quatro semânticas deste projeto (`acao`, `sucesso`, `informacao`, `atencao`) têm razão de contraste medida e escrita ao lado de cada uma (**DA-42**), e trocá-las por um token de fora jogaria esse trabalho fora sem que ninguém tivesse conferido o novo. É a mesma decisão tomada no convite da home, em 2026-08-27: aproveita-se a estrutura, não a aparência |
| DA-47 | Do redesign, **quatro propostas não foram implementadas** — e a razão de cada uma está escrita, em vez de a ausência ficar por conta de esquecimento | (1) **"Salvar e continuar depois"** é um rascunho gravado no servidor: tabela, rota e link assinado. É funcionalidade nova, não desenho de tela. (2) **Escolher a atividade já na programação** move a etapa 2 do formulário para a vitrine e refaz o caminho que 12 cenários de navegador percorrem — mudança de fluxo, não de layout. (3) **"4 passos viram 3"**, dissolvendo a Revisão no resumo lateral, apagaria a etapa onde mora o **aceite do regulamento**; o resumo lateral entrou, mas **somando-se** à revisão, não no lugar dela. (4) A **máscara automática do CPF** muda o valor que chega ao servidor — e é o CPF que detecta inscrição duplicada, com pepper. Merece mudança própria, não carona num redesenho |
| DA-48 | Dois textos da proposta **não foram copiados porque são falsos neste sistema**: "Pix ou cartão no fim" e o campo "Onde" com "Ver no mapa" | O sistema cobra **só por Pix** (Fase 8a), e `EventoPublico` **não tem campo de local** — não há endereço para mostrar nem mapa para abrir. Copiar os dois deixaria a tela mais parecida com o mockup e mentindo para quem lê. Ficou "o pagamento é por Pix, ao final", e a caixa "Onde" simplesmente não existe |
| DA-49 | A **home** também segue o modelo, e não só a vitrine — mas as duas continuam sendo **telas separadas** | O diagnóstico da proposta trata a tela de entrada como "página do evento", e os dois defeitos que ele aponta são da home: *"tela inicial vazia, nenhuma informação decisiva"* e *"**'Fazer inscrição' e 'Ver a programação'** têm o mesmo peso visual"* — esses dois rótulos são de lá. Entraram na home a grade de fatos, a etiqueta de prazo e uma só ação primária. **Não** entrou a fusão das duas telas: a home apresenta e encaminha, a vitrine tem programação, regulamento e formulário, e é essa separação que permite mais de um evento aberto ao mesmo tempo (**DA-38**). Juntá-las seria mudança de arquitetura, não de layout |
| DA-50 | **Vaga restante continua fora da home**, mesmo o mockup a mostrando | A decisão de 2026-08-27 não mudou de razão: na porta de entrada o número vira pressão sem contexto e desatualiza no segundo seguinte. Quem precisa dele vê na vitrine, **por atividade**, que é onde ele significa alguma coisa. Há teste que percorre o texto cru da resposta e falha se `vagas_disponiveis` ou `capacidade` aparecerem. O que entrou no lugar foi o **prazo** (`prazo_rotulo`): é fato do evento, é uma frase e não um contador, e não desatualiza enquanto a pessoa lê |
| DA-51 | **A identidade visual do `ccc-redesign.html` foi adotada inteira** — paleta, tipografia e formas —, e **só no lado público**. O painel administrativo continua no tema azul do studio | Decisão do dono do produto, e ela reverte o critério da **DA-46** ("aproveita-se a estrutura, não a aparência"): desta vez o que se adota é justamente a aparência. O motivo de o painel ficar de fora é o mesmo de sempre — são quarenta telas que ninguém pediu para mudar, e mexer nelas transformaria uma troca de tema numa etapa impossível de revisar. O trabalho foi partido em **três planos**: este é o tema e as peças de base, o seguinte é agenda e evento, o terceiro é o caminho da inscrição. Um cenário vermelho aponta para um plano só, e é essa a razão de serem três |
| DA-52 | O escopo do tema mora no **`<html>`**, escrito pelo Blade **e** pelo `app.ts`, e não numa `div` do `PublicoLayout` | Não é preferência: `SelectContent.vue` e `DialogContent.vue` **teleportam o conteúdo para o `document.body`** (portal do reka-ui). Escopo posto na subárvore da página não alcança a lista de cidades do formulário nem os diálogos — eles abririam com as cores do painel no meio de uma tela verde. E são precisos **os dois lados**: só o Blade erra toda navegação seguinte, porque o Inertia troca apenas o corpo e nunca reescreve o `<html>`; só o cliente **pisca** o tema errado no primeiro quadro. O cenário do portal em `tests/e2e/identidade-publica.spec.ts` foi **visto vermelho** com o atributo movido para a raiz do layout |
| DA-53 | `informacao` é **derivada**, e não emprestada do verde de ação | A identidade tem verde, âmbar e vermelho — três cores para quatro papéis. Ela usa o mesmo verde para ação e para sucesso, e isso foi aceito (o que distingue os dois passou a ser a **superfície**: cheia para "clique aqui", lavada para "está disponível"). Mas `informacao` é usada em dezenas de lugares para navegação e aviso neutro: pintá-la do mesmo verde faria "clique aqui" e "isto é um aviso" ficarem idênticos. Entrou um azul-petróleo frio, `#0C5A75`, com 6,87:1 sobre o papel |
| DA-54 | O **modo escuro continua existindo** no lado público, derivado da identidade nova | O protótipo é só claro. Tirar o escuro seria regressão para quem o usa por necessidade, e vai contra a linha da Etapa 23, que passou a respeitar "reduzir movimento". A variante foi derivada com contraste conferido contra os **dois** fundos escuros — a página e o cartão, que é o caso difícil para texto claro. **Se o dono do produto preferir o lado público sempre claro, é uma linha para reverter** — e a decisão é dele, não de quem escreveu a tela |
| DA-55 | Três tons do protótipo **reprovaram em AA e entraram escurecidos**, com os dois valores registrados ao lado (DA-42) | O âmbar `#E9922B` como **texto** rende 2,18:1 sobre o papel — é o mesmo caso que a DA-42 já resolveu uma vez, e a resposta foi acessibilidade; virou `#8A5310`, com 5,65:1. A borda de campo `#C7D0C6` rende 1,58:1 e **não é decoração**: é contorno de controle, e a WCAG 1.4.11 pede 3:1; virou `#7C8B83`, com 3,57:1. O cinza `#8A968E` da etiqueta "encerrado" rende 2,53:1 sobre o próprio fundo dela; passou a usar o `#5B6C64`, com 4,58:1. **O fio de separação continua claro de propósito**: ele não carrega texto nem delimita controle, e a norma não lhe cobra contraste |
| DA-56 | A **altura mínima de 48px** e a forma pílula do botão valem **só no público**; no painel o botão não muda | Mudar altura de botão no painel mexeria em quarenta telas que este plano jurou não tocar. E é `min-h`, não `h`: altura mínima deixa o rótulo quebrar em duas linhas no celular sem o texto vazar |
| DA-57 | O convite da home é um **cartão branco sobre o papel**, e não uma faixa colorida de borda a borda | Decisão do dono do produto, tomada depois de ver a tela pronta. A faixa usava o token de `informacao`, que a identidade nova derivou como tom frio — numa página cujo botão principal é verde-mata, as duas cores disputavam a mesma dobra e a tela passava a ler como duas identidades brigando. É também o que o próprio protótipo faz: nenhuma faixa, um cartão com sombra sobre o fundo papel. **A derivação de `informacao` continua de pé** — ela é usada em avisos neutros (o "guarde o link" da cobrança, as etiquetas de situação do acompanhamento), que é onde um tom frio distingue "isto é aviso" de "clique aqui" |
| DA-58 | O evento ganhou **onde ele acontece**: `local` (o nome curto) e `local_detalhe` (como chegar), com os dois campos na tela de cadastro | Decisão do dono do produto. "Onde" é uma das três perguntas que alguém faz antes de decidir se vai, e era a única sem resposta no sistema — quem se inscrevia descobria o lugar por fora, no grupo do WhatsApp. São **dois** campos porque são duas coisas: o nome curto cabe na linha ao lado da data, na porta de entrada; o detalhe (distância, referência, estacionamento) só faz sentido na página do evento. Um campo só obrigaria a escolher entre caber e ser útil. Os dois aceitam nulo, senão nenhum evento já cadastrado poderia ser salvo até alguém preencher |
| DA-59 | **Vaga restante passou a aparecer na porta de entrada** — o número e a barra de ocupação —, revertendo a **DA-50** | Decisão do dono do produto, tomada vendo a tela pronta. A ressalva que motivou a proibição **continua verdadeira** e está registrada: número de vaga na entrada vira pressão sem contexto e desatualiza no segundo seguinte. O que mudou foi o julgamento sobre ela — e esse julgamento é de quem encomenda a tela. Não custa consulta: são dois campos do próprio evento mais um contador que ele já mantém. O que **continua** fora são os contadores internos da reserva (`vagas_reservadas`, `vagas_confirmadas`), que são mecanismo (D-04) e não dizem nada a quem chega |
| DA-60 | A home passou de **uma para três consultas**, para mostrar os dois dias do evento | Decisão do dono do produto. O controller evitava carregar a árvore do evento de propósito: a home é a página mais acessada e a primeira que um pico de acesso encontra. As duas consultas novas — os dias e os grupos deles — são carregadas **só para o evento em destaque**, nunca para os outros abertos nem para o próximo, e por isso o número **não cresce** com a quantidade de eventos cadastrados. As atividades continuam de fora: a home resume o dia, não repete a programação da vitrine. O teste que vigiava "uma consulta" passou a vigiar "três", com o motivo escrito e com dias de verdade no cenário — senão ele nunca percorreria o caminho que existe para defender |
| DA-61 | A barra de vagas mede o que **já foi ocupado**, e não o que sobra — ao contrário do protótipo | É assim que se lê uma barra que enche. O protótipo desenha a barra cheia representando vaga livre; seguir isso deixaria as **duas** barras do sistema dizendo coisas opostas, porque a da vitrine já estava escrita antes. Consistência dentro do produto pesou mais que fidelidade ao arquivo |
| DA-62 | A **DM Mono é pedida, nunca deduzida**: o gancho é `font-mono` / `font-numero`, e não `tabular-nums`. E a regra vive em `@layer utilities`, não em `base` | Dois defeitos numa tacada só, os dois encontrados pelo dono do produto olhando a tela. **O primeiro:** `tabular-nums` quer dizer "algarismos de largura fixa", que é um recurso tipográfico que fontes proporcionais também têm — não quer dizer "monoespaçada". Com o atalho, o **preço** saía monoespaçado, e no protótipo ele é Bricolage Grotesque; junto com ele foi toda contagem e todo prazo da interface. **O segundo, pior:** a regra estava em `@layer base`, e utilitário do Tailwind vence base — então `font-mono` continuava caindo na monoespaçada **do sistema**, e a DM Mono nunca chegava justamente onde alguém a tinha pedido. O protótipo usa a monoespaçada em três lugares, e os três são onde caracteres precisam alinhar em coluna: horário da programação, opção de horário e código Pix |
| DA-63 | A tipografia dos dias e das etiquetas foi ajustada ao **número exato** do protótipo, e não "por aproximação" | Também apontado pelo dono do produto, e medido em vez de discutido: o espaçamento entre letras das etiquetas estava em 0,05em contra os 0,12–0,14em do arquivo — menos da metade —, o nome do dia em 16px contra 17px e sem o `-0,01em`, e o resumo em 14px contra 14,5px. Em texto curto e em caixa alta, o espaçamento entre letras é o que separa "etiqueta" de "frase pequena": errá-lo desmancha a hierarquia inteira sem que nada pareça quebrado |
| DA-64 | A página do evento ganhou **o que está incluído** e **perguntas frequentes**, em `jsonb`, com edição no admin | São as dúvidas que hoje a organização responde no WhatsApp toda semana — e que fazem a pessoa adiar a inscrição, o que aqui é o mesmo que desistir. Ficam em `jsonb` e não em tabelas próprias porque são listas ordenadas de texto que só existem para serem exibidas nesta tela: nenhuma regra depende delas, nada as relaciona com outra coisa. Uma tabela por lista traria duas migrations, dois models, duas telas de ordenação e nenhuma pergunta nova respondida. As duas aceitam nulo, e **a seção inteira some quando a lista está vazia** — título com nada embaixo não informa quem visita, informa que falta alguém preencher |
| DA-65 | Os valores de tipografia, cor e espaçamento da vitrine foram **copiados um a um** do protótipo, com o seletor de origem escrito ao lado de cada bloco | Pedido do dono do produto depois de dois erros que ele mesmo pegou (**DA-62** e **DA-63**), os dois nascidos de aproximar em vez de medir. Agora cada bloco diz de onde veio — `.fact__k`, `.day__t`, `.slot__t`, `.buy__p` — e o número está no código, não na lembrança de quem escreveu. A malha de linhas de 1px entre as caixas de fatos é o truque do arquivo: `gap:1px` sobre fundo da cor da linha, que evita borda dupla nos encontros sem que nenhuma caixa precise saber se é a primeira da fileira |
| DA-66 | A seção passou a se chamar **"Como funciona o fim de semana"**, e não "Programação" | É o título do protótipo, e ele explica em vez de rotular: quem chega pela primeira vez entende uma frase melhor que uma palavra sozinha. A âncora continua se chamando `titulo-programacao` — o endereço que o link da home usa não mudou, só o texto que a pessoa lê. Um cenário de navegador afirmava o rótulo antigo e foi atualizado, com o motivo escrito nele |
| DA-67 | O caminho da inscrição — formulário, revisão, cobrança e recuperação de acesso — passou ao desenho do protótipo, com os valores copiados um a um | Fecha o plano 3. Campos de 50px com borda de 1,5px e raio de 10px; a escolha de atividade virou o `.opt` (caixa desenhada de 22px, horário em coluna monoespaçada de largura fixa, linha inteira clicável); a revisão virou blocos `.rev` com faixa de título e rótulo em coluna de 148px; a cobrança ganhou o preço em Bricolage Grotesque, o prazo numa pílula âmbar e as instruções numeradas em círculos verdes. Cada bloco carrega no comentário o seletor de onde o valor veio |
| DA-68 | **Conflito de classe estática com classe condicional** foi a causa de dois defeitos, e o padrão passou a ser um ternário decidindo tudo | Havia `bg-background` fixo com `bg-acao` condicional no círculo da etapa, e `bg-card` fixo com `bg-muted/50` condicional na atividade indisponível. As duas classes chegam juntas ao elemento e quem decide é a **ordem no arquivo de estilo**, não a intenção de quem escreveu — o círculo da etapa atual ficava cor de fundo, com texto branco por cima, invisível. Onde o estado muda a cor, um ternário só decide fundo, borda e texto ao mesmo tempo |
| DA-69 | O "Editar" da revisão **mantém a aparência do protótipo e ganha a área de toque que ele não tem** | O arquivo desenha esse botão como texto puro, e assim ele mede 37×20 — abaixo dos 44px. Um cenário de acessibilidade acusou com o tamanho exato. A cor e o peso ficaram; a área cresceu com margem negativa, para a faixa do título não engordar junto. Fidelidade ao desenho para até onde a acessibilidade começa (**DA-42**) |
| DA-70 | **A barra de vagas passou a medir o que SOBRA**, revertendo a **DA-61** | O argumento anterior — "barra que enche se lê como preenchimento" — parecia bom e estava errado para este caso. O dono do produto corrigiu mostrando o desenho: aqui a barra é uma reserva que se esvazia, e cheia quer dizer "ainda dá tempo". As duas barras do sistema, a da porta da rua e a da vitrine, mudaram **juntas** — o motivo que sustentava a DA-61 era a consistência entre elas, e ela continua valendo, só que do outro lado |
| DA-71 | **Os links do lado público são verdes**, e o sublinhado ficou só onde o link mora dentro de uma frase | O protótipo declara `a { color: var(--mata); text-decoration: none }`, com sublinhado no hover; os nossos estavam azuis e sublinhados o tempo todo. A cor virou verde em todos. O sublinhado permanente **saiu dos links soltos** — os que ocupam a própria linha, num cartão — e **ficou nos que estão dentro de uma frase**, como o rodapé: ali ele não é enfeite, é o que distingue o link do resto do texto para quem não separa as duas cores (WCAG 1.4.1). Fidelidade ao desenho até onde a acessibilidade começa, como já valeu no "Editar" da revisão |
| DA-72 | O botão da vitrine passou a se chamar **"Fazer inscrição"**, como na porta da rua | É o texto do protótipo, e a mesma ação tinha dois nomes — "Quero me inscrever" aqui, "Fazer inscrição" lá. **Efeito colateral que a mudança criou e que o desenho não previa:** o Playwright procura por trecho, então o nome passou a casar também com o botão da própria home, e um cenário podia clicar duas vezes nela antes de navegar. O cenário ganhou uma espera explícita pela vitrine — o defeito era do teste, não da tela, mas nasceu daqui |
| DA-73 | Quando o dia tem **um grupo só**, o título do bloco é o nome do grupo, e o nome do dia não aparece | No protótipo o bloco se chama "Modalidades esportivas". Aqui saíam os dois empilhados — "Dia 1 — Esportes" e, logo abaixo, "Modalidades esportivas" — dizendo quase a mesma coisa, com a data já na linha verde acima. Com mais de um grupo no dia o nome dele volta: é ele que explica o que aqueles blocos têm em comum. A regra de escolha saiu da etiqueta flutuando à direita e entrou no corpo do texto, junto da explicação |
| DA-74 | O cabeçalho de toda tela pública ganhou **duas portas: "Agenda" e "Minha inscrição"** — e a marca continua sendo o brasão da CCC, sem o monograma do protótipo | O `.top__nav` do protótipo. Até aqui o caminho para "já me inscrevi, perdi o link" só existia na home, e quem chega ao formulário por um link direto do WhatsApp — que é como quase todo mundo chega — não tinha como voltar a ele sem digitar um endereço. O monograma ficou de fora porque **marca é assunto de quem conduz a comunidade, não da tela** (DA-37). No celular a navegação desce para uma segunda linha inteira em vez de espremer o nome da comunidade: a 320px os dois não cabem lado a lado sem que um vire reticências. E o link ganhou 44px de altura, contra os 33px do desenho, porque alvo de dedo tem piso (DA-42) |
| DA-75 | O campo de nascimento passou a ser **digitável com máscara `dd/mm/aaaa` e calendário próprio**, ao custo de uma dependência nova (`@internationalized/date`) | Decisão explícita do dono do produto, tomada sabendo do custo. O `<input type="date">` que estava aqui era acessível e não pesava nada, mas entregava a aparência ao navegador — cada um com a sua — e, no computador, escrevia a data no formato do sistema operacional, e não no do desenho. **O contrato não mudou:** o campo recebe e emite ISO (AAAA-MM-DD), que é o que o `StoreInscricaoRequest` espera; nada fora dele soube da troca. O que a troca **cobrou** foi a conferência que o campo nativo dava de graça: 31/02/2000 podia, agora, ser digitado. Por isso o conferidor de `data_nascimento` passou a recusar data inexistente e data no futuro, com frase em português, no mesmo lugar dos outros erros. `Popover` e `Calendar` entraram **adaptados à mão**, e não pelo gerador do shadcn-vue (DA-44), com o dia medindo 44px em vez dos 32px que o gerador escreveria |
| DA-76 | **A barra do rodapé no celular fica**, contra o protótipo, que põe as ações só dentro do painel | Decisão do dono do produto. No celular não existe resumo lateral — ele só aparece a partir de 1024px —, então o Total simplesmente não teria onde aparecer no formulário, e o "Continuar" ficaria atrás de uma rolagem inteira de campos. A barra é a única coisa que responde "quanto vai custar?" enquanto a pessoa digita. Ela agora mora **dentro do painel**, com margens negativas até a borda dele, para ser lida como rodapé do formulário e não como mais uma caixa solta |
| DA-77 | A nota do nascimento continua dizendo **"idade mínima ou máxima"**, e não só "mínima" como no protótipo | O sistema aceita as duas regras e **recusa a inscrição pelas duas**. Copiar a frase do desenho faria a tela mentir sobre a própria validação, e quem fosse barrado por idade máxima não teria lido nada a respeito antes. Fidelidade ao desenho até onde ela começa a enganar quem lê — o mesmo critério da DA-69 e da DA-71 |
| DA-78 | O cartão **"Precisa de ajuda?" saiu da lateral** do formulário | O mesmo telefone e o mesmo e-mail já estão no rodapé de toda tela pública, e o protótipo só traz esse bloco na vitrine. Dizer duas vezes na mesma tela não ajuda ninguém: o que o bloco fazia, na prática, era empurrar o Total para baixo da dobra. Junto com ele saiu o título "Resumo" — o desenho abre o `.summary` pelo **nome do evento**, com "{quando} · {local}" embaixo, e uma palavra que só diz o que a caixa é não informa quem está olhando para ela |
| DA-79 | O `.panel` mora **no `Criar.vue`, envolvendo as três etapas**, e não dentro de cada componente de etapa | O plano previa tocar `PassoParticipacao.vue` e `PassoRevisao.vue` para pôr cada um na sua moldura. Seria a mesma caixa escrita três vezes, com três chances de os valores divergirem — e foi exatamente assim que a DA-63 nasceu. A moldura é da **tela**, não da etapa: quem troca de etapa continua no mesmo painel, que é o que o protótipo desenha. Efeito colateral bom: os dois componentes não precisaram mudar uma linha, e o título da etapa (que recebe o foco a cada troca) virou o `h2` do painel, com os 23px do desenho |

---

## Pendências

| # | Pendência | Responsável |
|---|-----------|-------------|
| P-01 | ~~Escolher o provedor de pagamento real~~ — **resolvida na Fase 8a** (decisão **DA-16**, 2026-08-27): **Efí**, API Pix. Implementada e provada com 36 testes, sem alterar nenhuma regra de inscrição | Concluída |
| P-02 | Definir política de reembolso — **continua aberta**, e agora ela também segura o estorno pela Efí: `refundPayment()` lança "não suportado" de propósito (decisão **DA-18**), e o `endToEndId` que a devolução vai exigir **já está sendo guardado** desde a Fase 8a, para que a decisão não chegue com um passivo de pagamentos impossíveis de devolver. A Fase 6b permite cancelar inscrição já confirmada, e esse cancelamento **não devolve dinheiro** (decisão D-56): o pagamento segue registrado como recebido e a devolução, se houver, é combinada entre pessoas | Dono do produto |
| P-03 | Definir o que fazer com pagamento recebido após o prazo — **continua aberta**. Enquanto não houver decisão, a confirmação manual **recusa** inscrição já expirada, porque a vaga voltou para a fila e pode ter dono novo (decisão D-57) | Dono do produto |
| P-04 | Definir prazo de retenção e descarte de dados pessoais | Dono do produto |
| P-05 | ~~Definir como o participante acessa a inscrição depois~~ — **resolvida na Fase 5a**: URL assinada com validade casada com o prazo de pagamento, mais 24 horas de folga para o participante ainda ver a tela de "prazo vencido" em vez de um 403 sem explicação | Concluída |
| P-06 | Confirmar **com o comercial da Efí** a taxa Pix efetiva, as tarifas fixas de conta e o prazo de liquidação — **continua aberta**. O valor público de **1,19% por transação** está registrado em `PAYMENTS.md` com a data da consulta, mas valor de página pública não é proposta escrita. **Não bloqueia a operação**: nenhuma taxa entra em código ou configuração (decisão **DA-23**). Bloqueia a precificação do evento | Dono do produto. Ver seção 6 de `PAYMENTS.md` |
| P-08 | Conferir no `.env` local as chaves `PAYMENT_GATEWAY`, `PAYMENT_FAKE_SIMULATION_ENABLED` e `PAYMENT_FAKE_WEBHOOK_SECRET`, como já estão em `.env.example`. Sem o segredo do webhook, o provedor simulado recusa todo aviso (falha para o lado seguro) | Pessoa desenvolvedora, na própria máquina |
| P-07 | Ajustar o arquivo `.env` local para `DB_PORT=55432` e `FORWARD_PGSQL_PORT=55432`, como já está em `.env.example` (decisão D-19) | Pessoa desenvolvedora, na própria máquina |
| P-09 | ~~Corrigir os **20 erros de verificação de tipos** revelados pela decisão D-43~~ — **resolvida na Fase 6a** (decisões D-52, D-53 e D-54): `npx vue-tsc --noEmit` termina com **zero erros**, e o projeto passou a ter verificação de tipos rodando limpa pela primeira vez. Nenhum erro foi silenciado; um deles era defeito de verdade (o item ativo da barra lateral nunca acendia) | Concluída |
| P-11 | Subir o **`tailwind-merge` para a 3.x**. A versão instalada é a **2.6.0**, feita para o Tailwind 3: ela não reconhece a sintaxe `w-(--variavel)` como sendo uma classe de largura e, por isso, deixa de apagar a classe concorrente que o componente do shadcn traz de fábrica. **O que destrava:** remover o `!` da linha 43 do `Sidebar.vue` (decisão **D-86.1**), que hoje é o que segura a gaveta do celular nos 288px declarados. **O que exige:** é plano próprio, não um ajuste — o merge 3.x muda o comportamento de várias famílias de classe, e a troca precisa ser conferida nos **23 componentes de interface**, com a suíte de navegador rodando. **Não é urgente:** as dez ocorrências de sintaxe nova foram conferidas uma a uma, e **a gaveta do celular é hoje o único lugar do projeto onde essa disputa acontece** — este número existe para que a próxima pessoa não precise refazer a conferência | Pessoa desenvolvedora |
| P-12 | `npm run format:check` **reprova 52 arquivos** em `resources/`. Não é regressão de nada: a lista é idêntica, arquivo por arquivo, antes e depois da correção **D-86** (as duas saídas foram comparadas e são iguais byte a byte). É faxina de formatação acumulada, provavelmente de quando o `prettier-plugin-tailwindcss` passou a reordenar classes na Etapa 21. Fica registrado com o número para deixar de ser folclore: quem rodar o comando e vir 52 avisos precisa saber que eles já estavam lá. **A correção é `npm run format` num commit só, sem nenhuma outra mudança junto** — misturado com conserto, ninguém conseguiria revisar | Pessoa desenvolvedora |
| P-13 | A barra de ações do formulário no celular está declarada `sticky bottom-0`, mas **não gruda**: a raiz do `PublicoLayout` tem `overflow-x-hidden`, e `overflow-x: hidden` faz o navegador computar `overflow-y: auto` — o elemento vira um contêiner de rolagem que nunca rola, e a posição pegajosa passa a ser medida contra ele em vez da janela. Medido no navegador: a barra desce junto com a página, ponto a ponto. **Não é regressão desta etapa** — as duas peças (o `overflow-x-hidden` da raiz e o `sticky` da barra) já conviviam antes dela, e a barra continua existindo, com o Total e o "Continuar" juntos, no fim do formulário. **O conserto não é de uma linha:** tirar o `overflow-x-hidden` mexe na guarda que impede rolagem horizontal a 320px, que é critério provado em dois cenários; trocar por `fixed` exige devolver ao corpo da página o espaço que a barra passa a cobrir. Fica registrado para quem for decidir | Dono do produto e pessoa desenvolvedora |
| P-10 | Decidir se a varredura de inscrições vencidas **sob demanda** — a que roda na hora da inscrição, quando o contador diz "lotado" — deve ganhar um teto por chamada (por exemplo, as 100 mais antigas), deixando o resto para a rotina agendada. Hoje ela varre tudo, e a medição mostrou que 2.000 vencidas de uma vez levariam 18 segundos, **com a pessoa que está se inscrevendo esperando**. Não foi implementado porque mudaria regra de negócio. Ver a seção 4.6 de `docs/PERFORMANCE.md` | Dono do produto |

---

## Revisão cruzada dos documentos (etapa 3)

Verificações feitas nos sete documentos, com o resultado:

| Verificação | Resultado |
|-------------|-----------|
| Nomes de tabela iguais em todos os documentos | Sem divergência |
| Situações de inscrição e de pagamento iguais em todos os documentos | Sem divergência |
| A palavra "pago" nunca aparece como situação de inscrição | Confirmado: só aparece como situação de pagamento ou na justificativa da decisão D-03 |
| Nenhum identificador de banco com acento ou cedilha | Confirmado |
| `float`/`double` só aparecem como o que **não** se deve usar | Confirmado |
| `lockForUpdate()` só aparece como prática proibida, com o motivo | Confirmado |
| Regra de capacidade descrita igual no PRD, na arquitetura, no banco e nas regras | Sem divergência |
| Mapeamento dos oito testes exigidos pelo briefing | Presente em `BUSINESS_RULES.md` |
| Diagramas Mermaid válidos | Os 11 diagramas foram renderizados com `@mermaid-js/mermaid-cli`; todos passaram |

Nenhuma contradição exigiu correção. As decisões D-13 a D-18 foram registradas durante a redação dos documentos técnicos.

**Sobre as taxas de gateway:** apenas a Efí publica os percentuais em página aberta. Pagar.me apresenta preço negociado, e Mercado Pago e Asaas não expõem valores em conteúdo público acessível. Essas células ficaram como **"a validar"**, conforme a regra de nunca registrar número comercial não confirmado.

---

## Dependências externas adicionadas

| Pacote | Onde entra | Por quê |
|--------|-----------|---------|
| `bacon/bacon-qr-code` | `app/Services/Pagamentos/GeradorQrCodePix.php` | Desenha o QR Code do Pix como SVG no próprio HTML, sem `imagick` no servidor e sem peso no pacote que o celular baixa |
| `@playwright/test` (desenvolvimento) | `tests/e2e/` | Prova o caminho do participante num navegador de verdade, imitando um celular |
| `efipay/sdk-php-apis-efi` | `app/Services/Payments/Efi/EfiClient.php` — e **só ali** | O SDK oficial da Efí, escolhido pelo dono do produto (decisão **DA-21**). Ele fica atrás de um embrulho fino porque usa cliente HTTP próprio, que as ferramentas de teste do Laravel não interceptam: sem o embrulho, provar a emissão de cobrança exigiria credencial, certificado e rede. Nenhum outro arquivo do sistema o instancia |
| `resend/resend-laravel` | `config/mail.php` e `config/services.php` — o transporte de e-mail de produção | O que faz os cinco e-mails da Fase 7 chegarem a alguém (decisão **DA-29**). A entrega vai por **API HTTPS**, e não por SMTP, porque as portas 25 e 587 costumam vir bloqueadas em servidor de nuvem — e a falha seria silenciosa: o trabalho ficaria na fila e ninguém receberia nada. Em desenvolvimento nada muda, o e-mail continua parando no Mailpit |
| `spatie/laravel-permission` | `app/Models/User.php` (trait `HasRoles`), `database/seeders/PapeisSeeder.php`, `bootstrap/app.php` (apelidos `permission` e `role`) e o grupo de rotas `/admin` | Dá papéis e permissões ao lado administrativo. Escrever isso à mão significaria reimplementar tabelas, cache de permissões e middleware — um pacote maduro inteiro — para chegar exatamente ao mesmo lugar. Ver a decisão **D-50** |
| `@tailwindcss/vite` + `tw-animate-css` | `vite.config.ts` e `resources/css/app.css` | O que substitui, na versão 4 do Tailwind, o par `tailwindcss` + `autoprefixer` no PostCSS e o `tailwindcss-animate`. Os dois antigos **saíram** do projeto (decisão **DA-39**) |

O `@vueuse/core`, que já vinha no pacote inicial, resolve a cópia do código Pix e o contador regressivo.

**A Fase 5b não adicionou nenhuma dependência**, nem no PHP nem no navegador: o envio do e-mail é o `Mail` do próprio Laravel, o QR Code da segunda via reaproveita o gerador da Fase 5a e a linha do tempo é HTML e Tailwind sobre os componentes que já existiam.

**A Fase 6a adicionou uma só**, o `spatie/laravel-permission`. O painel não trouxe nenhuma biblioteca de gráfico nem de tabela: os números são texto grande com explicação ao lado, e a tabela de vagas é HTML com cabeçalho de linha e de coluna — que é o que um leitor de tela consegue ler e uma biblioteca de gráfico, não.

**A Fase 7 não adicionou nenhuma dependência**: os e-mails são o `Mail` do próprio Laravel, os corpos são Blade, a fila é a do framework sobre o Redis que já subia no Sail, e o Mailpit (painel em http://localhost:8025) já vinha no `docker-compose.yml` desde a Fase 0.

**A Fase 6b não adicionou nenhuma dependência**, nem no PHP nem no navegador. Os cadastros são formulários do Inertia sobre os componentes que já existiam, a exportação em CSV usa a resposta em fluxo do próprio Laravel (nenhuma biblioteca de planilha entrou), e o diálogo de motivo obrigatório é o componente de diálogo que o projeto já tinha.

**A Fase 8a adicionou uma só**, o SDK oficial da Efí — e **nenhuma migração**. O identificador da transferência coube no campo `jsonb` que já existia em `pagamentos`, o QR Code continua sendo desenhado pelo gerador da Fase 5a a partir do texto que a própria Efí devolve (nenhuma segunda ida à rede só para buscar imagem), e o cache do token de acesso usa o Redis que já subia no Sail.

**A Etapa 19 (implantação) adicionou uma só**, o `resend/resend-laravel` — e **nenhuma migração**. Tudo o mais é arquivo de infraestrutura fora do PHP: `Dockerfile`, `docker/Caddyfile`, `docker/entrypoint.sh`, `docker/compose.portainer.yaml` e o workflow do GitHub Actions. A extensão `redis` do PHP, que a fila e o cache exigem, é instalada na imagem, não é pacote do Composer.

**A Etapa 21 trocou dependências em vez de acrescentar**: saíram `tailwindcss@3`, `tailwindcss-animate` e `autoprefixer`; entraram `tailwindcss@4`, `@tailwindcss/vite` e `tw-animate-css`. **Nenhuma migração, nenhum arquivo de domínio alterado, e nenhuma dependência nova no PHP.** O `radix-vue` continuou onde estava, de propósito — a troca por `reka-ui` era o plano B, feito na Etapa 22 (**DA-43**).

**A Etapa 22 também trocou em vez de acrescentar**: saiu `radix-vue@1`, entrou `reka-ui@2` — é o mesmo projeto de primitivas, renomeado na versão 2 (**DA-43**). As duas nunca conviveram no `package.json`, e não devem conviver: duas bibliotecas de primitivas no mesmo bundle significam dois comportamentos para a mesma caixa de marcar. **Nenhuma migração, nenhum arquivo de domínio alterado, e nenhuma dependência nova no PHP.**

**A Fase 9 não adicionou nenhuma dependência.** O registro de auditoria é uma tabela e um model; o seeder de volume usa inserção em lote do próprio Laravel; os limites de requisição e os cabeçalhos de segurança são recursos do framework (`RateLimiter` e um middleware escrito à mão); e o teste de carga são processos de sistema operacional, como já eram os seis do `ConcorrenciaTest`. A única mudança em dependência foi a **atualização** do `esbuild` (decisão D-85), que já estava no projeto.
